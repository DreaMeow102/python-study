# ================= Python =================
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# ================= 编译产物 / 可执行文件 =================
# 这些是从虚拟机或本机编译出来的，不该进版本库
*.exe
*.out
*.o
*.obj
*.so
*.dll
a.out

# ================= 编辑器 / IDE =================
# 如果你想同步 VS Code 的启动配置，把下面这行删掉
.vscode/
.idea/
*.swp

# ================= Windows =================
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# ================= 临时文件 =================
*.tmp
*.log
~$*

# ================= 私人备忘（不进仓库） =================
# 学生自己写的《愿景》，只留在本机。
# 2026-09-22 定：它放在 python学习\ 下，但永远不提交。
#
# 教训：先前把它放进 .git\ 目录想"藏起来"——那是错的。
#   .git\ 是 git 的内部数据库，git 完全不管里面的文件，
#   换机器 clone / 备份时跳过 .git / 仓库重建 —— 三种情况都会直接弄丢它。
#   正确做法就是这里：**放在正常文件夹，用 .gitignore 挡住**。
#
# 用不带路径的写法：这样它被移到哪个子文件夹都依然被忽略。
愿景.md

