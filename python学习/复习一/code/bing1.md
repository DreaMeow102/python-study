**问**：两行分别输出什么？**为什么 `nums` 没变？**（你这个就已经暴露了nums的实际输出了。）
nums输出为10 20 30 40 50
part输出为999 20 30 40
用c#的话来回答。因为part不是“继承”，是“新起切片”
如果有一个a = nums，就类似为继承，它相当于是在原有的基础上加东西和继承数组名字，但不代表它就是新东西。
所以两行输出，如我所说的那样。
至于为什么part输出没有50——因为它只取到4，4这个位值本身不取，到3。