#这题比较考验算法。而我算法较差。我的实现方式如下,并确保从中有我经过后再删除注释的代码，以展现我的思路：
s = input ("请输入你想转化的秒数，输入整数。")
# m = int(s) // 60
# h = m // 60
# m1 = m - (h * 60)
# s1 = int(s) % 60 
# print(f"{a}秒={h1}时{m1}分{s1}秒。")
h = int(s) // 3600
# m =(int(s) // 3600) % 
m = (int(s) % 3600) // 60
s1 = int(s) % 60
print(f"{s}秒={h}小时{m}分{s1}秒")

# 算法可没把我憋坏了，艹，这玩意本来是60进制，小时还是24小时进1天，有时候真的挺难绷的——高中时期可能我还有这个脑子