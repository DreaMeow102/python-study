s = "hello world"
b = 0
# for a in range(s):
    # if a = "l":
    #     b = b + 1
# for a in :
for a in s :
    # if a == l:
    if a == "l":
        b = b + 1
print (b)
# 这几部还是错的有点多。有些地方想当然了，而且range函数不应该这样用（他应该是直接建立了循环组，而不是和for这样进行循环本身）