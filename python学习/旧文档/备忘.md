# 备忘（会话流水 + 决策记录）

> **📖 指针** ｜ 本项目活文档见 `接手.md` §6 文档地图（**以那份为准，别抄这份的清单**）。
> `旧文档\` 一律作废，**不要以它为准**；`愿景.md` 是学生私人文件，**不参与阅读，教师勿动**。
>
> **本文档 = 会话流水 + 决策记录**（只追加，最新在最上）
> **不装**：当前状态 → `进度.md`｜规则 → `学习规定-AI.md` / `约定-学生.md`

> **这份文档是"墙"。**
>
> | | |
> |---|---|
> | **谁写** | 教师 AI |
> | **什么时候写** | **每个会话结束前必须写一条**（见 §一） |
> | **怎么改** | **只追加。** 旧条目不改（错了就在下面加一条更正的） |
> | **谁读** | **教师 AI 开工前必读最新一条**（接手.md §3 第 5 步）；§二决策记录**平时不用读**，只在"这条规则当初为什么这么定"被质疑时查 |
>
> **为什么它必须是"墙"触发器**：
> 环境层面的护栏（`fs-observation-policy`）只强制**写前必读**——它管不住"出题前该读 `进度.md`"。
> 而**要写一条像样的会话条目，就必须先知道上一条说了什么**——写这个动作，把"读"重新绑回了开工时。
>
> **⚠️ 纪律**：条目里**必须有证据**（跑过什么、输出是什么、几项通过）。
> "我检查过了"不算——**教师 AI 对自己判断的自述，尤其不可靠**（`bool("0")` 那次就是自述了一个诊断，而且是错的）。
>
> **最后更新**：2026-09-22

---

# 一、会话流水（最新在最上）

> **每个会话结束前加一条。** 格式见 `模板.md` §六。
> 条目要短，但**"这轮读了什么 / 体检结果 / 没结的"三格不能省**——它们是下个会话的接手要点。

### 2026-09-22 · 会话：★★ **出题总纲重写** —— 「锚点是知识点+例题，不是学生」

**这轮读了**：`第六课-正文.md`（题面区 grep + 4 处定点读）· `复习二-正文.md`（题面区 grep + 6 处定点读）· `学习规定-AI.md` §2
**体检**：跑了 —— **通过 36 · 失败 0 · 警告 11**（9 项）· **对账 绿（第六课 13/13）**

**学生定的总纲（原话）**：
> 「出题应该考的是"**知识点是否覆盖到位**"、和**例题是否重复**、"**知识点教到位了没有**"
> ——（**而不是制造学生不存在的错误，或者按照学生习惯来出题**，这样不行
> ——正常的出题都是**以知识点和例子为主，不是以学生为主**）」

**做了什么**：
- **新增 `学习规定-AI.md` §2.0（总纲，放在 §2 最前）**：三问（①覆盖到位吗 ②和例题重复吗 ③正文教到了吗、教在哪节）
  + **分工表**：**出题锚点 = 知识点 + 例题（学生 ❌ 不作为设计依据）**；**批改锚点 = 他的代码 + 他的错** ✅
  + **⛔ 六条"不许把学生请进题面"**（制造错误 / 预告他会犯错 / 按他习惯提要求 / 给方法名 / 替他划答案框架 / 假定他会抄）
- **§2.6 降级**：它管的是"这题值不值得留"，**不是"这题该怎么写"**——不许因为它把"他可能怎么错"写进题面
- **§2.1 补"位置"那一半**：① 语法点亮了吗 ② **正文里讲它的那节在题前面吗**（「本课要教」≠「题前已教」）
- **§2.13 删除**（43 行）——和 §2.0 重复，且**编号插到了 §2.12 前面**（顺序错）。唯一不重复的"留痕正确定义"并入 §2.0
- **「注释变少」那条挪进 §4.9**（它讲的是批改该看什么信号：**只追"这题你跑了吗"，不追注释数量**）
- **★ 全库清题面（11 处）**：第六课 4 处（E1「真的 `int()` 一遍」不再要求抄报错原文 / H2 第 5 问不再要求抄 SyntaxError /
  D3 删"注意第一个元素前不能有 `-`" / G2 标题删"三个坑"）；复习二 7 处（甲1/甲5/乙2/乙3 的"坑"→"要求"、
  甲6 删两条"提示思路"、乙2 删两条预告坑、乙4 删方法名提示）
- **`易错点台账.md` 用途收窄**：**批改时归因用**；**出题时只用来核对覆盖**，不拿它设计题面（`接手.md` 地图 + 台账头部同步）

**估错了第 5 次**：我说「第六课 1600 行大部分估计都有这种问题」——**实测 11 处**，占全部题面的比例不大。
**但类型是对的**：这是"题面格式"层面的病，不是个别题。

**没结的（要学生拍）**：
- ⚠️ **第六课正文里印着 17 个【自检】输出**（复习二的正文**没有**，输出只在答案里）。
  印在题旁边的后果：他**可以不跑，直接读答案**——和「我自己跑了才会发现」冲突。要不要也搬到答案里？

**下一个会话接手要点**：
- 出题前先读 **§2.0 三问**；**题面里任何一句，改不成"这题要求你做到 X" 就删**
- `学习规定-AI.md` **799 行**（贴着 800 硬警戒线）——**再加东西就得先删**

### 2026-09-22 · 会话：★ **学生被第一题卡住** —— 查出「题序错」这一类缺陷

**这轮读了**：`第六课-正文.md` §1 + A1 + 各组与各节的行号对照 · `第六课-参考答案.md` A1
**体检**：跑了 —— **通过 37 · 失败 0 · 警告 10**（新增检查「题序」，9 项）· **对账 绿（第六课 13/13）**

**学生原话（他是对的）**：
> 「第六课第一问我就被卡住了。（**我们教过 upper 吗？**）在我的印象里，小写改大写这边根本就没教过，
> 更没教过将一个字符串的单字母（尤其是首字母）改成大写」
> 「（这题怎么就超了？）我记得以前课程最多就是字符串的删减，或者取 -1 的倒写值才对」

**核查结论：他说的三句全对。**

| 他的疑问 | 事实 |
|---|---|
| 教过 `upper` 吗 | **没正式教过**。只在 §1.2 第 112 行当过一次**例子**（演示"要接住新串"）；**正式讲它在 §3.2 第 329 行** |
| 教过首字母大写吗 | **全课没有**。§3.2 只把 `capitalize()` 列为"了解级"一行 |
| 以前只学过切片/负索引 | 对 |

**A1 在 146 行，`upper` 的正式教学在 329 行 —— 差 180 行。**

**这不是"知识超纲"，是三重错**：① **顺序错**（题排在教它的节前面；核了 8 个组，**只有 A 组错位**）；
② **抄例题**（做法一 `"L"+word[1:]` ＝ §1.3 例题 `"b"+s[1:]` 换字母，正是 §2.2 说的"改数字就能过"）；
③ **没教过的组合**（首字母大写全课无出处）。

**为什么所有检查当时都是绿的（两个口径都对不上）**：
- 检查「知识点 ID」查的是"`upper` 在 `进度.md` §1 点亮了吗" → 4.7，属本课 → 判不超纲。
  **但"本课要教" ≠ "题之前已经教过"。**
- 检查「出题抄例题」查的是"例题和习题用了同一个**串**" → `"cat"` vs `"lemon"` **串不同** → 不报。
  **它看不见"结构相同"。**

**还有一条更该认的**：独立审核子模型**当天就写明了**——「A1 部分（**做法一＝正文 §1.3 只换字母**）」。
我把它归进"建议改"，**只动了判分口径，没动题面**。**它说了，我没做。**

**做了什么**：
- **A1 重做**：`upper` 全部拿掉；**新增第 3 步"把原串也打出来"**（`lemon`）作为"字符串没变"的证据。
  这一步 §1.3 例题**没有**，所以靠**新边界**站住 §2.2。首字母大写留给 §3.2 之后。
  答案同步改成**只有代码 + 输出**（§2.12）
- **新增 `体检` 检查「题序」（第 9 项）**：习题里用到的 `x.方法(`，若本课**有某一节标题专门点了它的名**，
  那一节必须排在题**前面**。**证伪试验通过**（合成一份同类缺陷 → 当场报出；删掉 → 恢复绿）。
  ⚠️ 第一版**边界写错**（取到"下一组"，把中间整节讲义算进题里）→ **27 条全假报**。修成"到下一个节标题/第 N 节/下一组为止"才干净
- `学习规定-AI.md` §6.0 权威表 + `接手.md` 地图同步（8 项 → 9 项）
- **★★ 学生又指出一条规定本身有问题**：A1 那条「**先写** `word[0]="L"` 跑一次……注释保留 + 抄报错原文」
  是照 `模板.md` 原第 105 行的指示写的，但它的性质是「**假定他会犯错**」。
  学生原话：「**留痕是我留自己错误的痕迹，不是假定我错误**……不该是为了留痕而留痕」
  「我从例子**已经知道了他会报错**，为什么我自己要写一遍还要摘报错？」
  → **`模板.md` 那条指示直接翻转为禁止**（加了「⛔ 硬规矩：留痕 ≠ 制造错误」）；
  **`学习规定-AI.md` 新增 §2.13**；`约定-学生.md` 方针表加 4b 行；**A1 去掉那一步**（正文与答案都改）。
  **并确认：全项目没有一条规定拿"注释数量"说事**——6 份批改里注释只作**证据**出现。
  他真实且唯一的失败模式是**懒得跑**（第二课 B2/B5、第四课 B4 三处）。
  **教训**：`模板.md` 那句把一个"学生自己的习惯"（`约定-学生.md`「学生自己定的学习方针」第 4 条）
  **写成了"教师对他的要求"**——**保留规则被误写成生产规则**。

**没结的**：
- ⚠️ **检查「题序」查不了"没被任何节标题点名"的方法**（如 `isdigit` 藏在"§5.1 四个方法"里）——那部分仍靠人工
- ⚠️ **检查「出题抄例题」查不了"结构相同、串不同"**（A1 就是这种）——这一条**目前只能靠独立审核**
- ⏸ 学生手上的 `第六课\code\a1.py` 是按旧题面写的，**要他重做**

**下一个会话接手要点**：
- **出题时逐题问一句**："这道题要用的方法，在正文里**排在哪**？"——别只看"本课教不教"
- **审核子模型的"建议改"也要落实**，别只挑"必须改"（这次就是这么漏的）

### 2026-09-22 · 会话（续）：清单归属定案 —— **由教师提取**

**这轮读了**：`复习一\code\bing3.md` · `复习一\code\_清单.md` §三 · `复习一-批改.md` §① · 全仓库「清单」命中 60+ 处逐条核对
**体检**：跑了 —— **通过 36 · 失败 0 · 警告 10**（与上一条同）

**做了什么**（学生确认：「**清单这边是由教师提取**」）：

- **查明复习一正文为什么还写着"学生自己写"** —— **因为那条规则就是学生在复习一当轮提的**：
  `复习一\code\bing3.md`：「我建议你来从我写过的代码注释中提取，而不是我自己来……我不太希望'感动自己'」，
  `复习一-批改.md` 第 ① 条已经接受。→ **原文是留痕，不是遗漏**，所以**不抹掉**；
  改为在正文**开头**（第 2 条"要交两样东西"下面）加更正框 —— **别让读者读到第 416 行才发现**。
- **复习二正文 3 处**"记进清单" → **"写进代码注释"**。那份正文写在规则变更**之后**，**没有留痕价值，直接改对**。
- **`学习规定-AI.md` §5.1 要点④** + **`约定-学生.md` 规则九（总表行 + ④）**：把**学生的动作**统一成
  **"写进代码注释"**，并注明"清单由教师从这里提取"——否则学生会去找一份**不存在**的"自己写的清单"。

**改了什么文件**：`复习一\复习一-正文.md` · `复习二\复习二-正文.md` · `学习规定-AI.md` · `约定-学生.md` · `进度.md`（待议 6 定案移出）· 本文件

**验证**：
  - 命令：`.\体检.ps1` → 绿 · 通过 36 · 失败 0 · 警告 10
  - 命令：全仓库 grep「清单」（排除 `旧文档\`）→ 逐条核对**主语**：剩下写"学生自己写"的**只剩复习一正文**，而它上面就是更正框 ✅

**没结的**：无新增

**下一个会话接手要点**：
- **`_清单.md` 永远由教师写**（`复习二\code\_清单.md` 是空骨架，批改时才填）
- **学生侧的动作是「写注释」，不是「写清单」** —— 出复习课时别再把清单当学生交付物

### 2026-09-22 · 会话：全库引用清扫（**体检绿 ≠ 健康**）

**这轮读了**：根目录活文档 7 份（`接手` / `学习规定-AI` / `约定-学生` / `进度` / `易错点台账` / `模板` / `备忘`）+ 各课抽查；`愿景.md` **按规矩没读**
**体检**：跑了 —— **通过 36 · 失败 0 · 警告 10**（8 项检查）· 改前是 **33 / 0 / 10**
**对账**：跑了 —— **绿 · 90 / 95**（5 题「整理过」跳过）· 改前 **88 / 93，且 `真题实战\` 完全没被覆盖**

**做了什么**（起因：学生问"根目录文档健康度是不是不足" → 结论 **是**）：

- **查出 45 处引用级缺陷，而体检打的是全绿。** 根因：2026-09-22 那轮重构（拆 约定.md / 砍语法速查 / 删旧检查 3）**改了结构，没做全库引用清扫**。
- **★ 架构决定：`规则N` 不废除，改成"必须登记在册"。** 它全仓库 100+ 处，**含已交付给学生的课次文件**；全改成 `§N.M` = **改教学内容**，代价大于收益。→ 在 `学习规定-AI.md` 开头加**「规则编号对照」12 行表**，并由检查「引用标」**机器校验**。旧编号从此可解析、可检查、**不用动交付物**。
- **`体检.ps1` 五处升级**：① 检查项改用**稳定「键」**（序号漂移过一次，全库 5 处指错一位）；② 加 5b/5c/5d —— 检查「<键名>」与 `规则N` 必须登记，残留的 `检查 N` 报警；③ 检查「课次表」不再对 `⬜ 待写` 报**永久假黄**；④ 检查「体量」**加报课次文稿体积**（`第六课-正文.md` 1631 行，原先在警戒线外没人看见）；⑤ 新增 `<尖括号>` 占位符抑制器。
- **`对账.ps1` 补掉一个"假绿"**：它按 `<课次名>-参考答案.md` **拼串**找答案，而真题卷叫 `真题实战一-参考答案.md` → **找不到就静默跳过，还错报"已知例外：第一课"**，汇总照样绿。**真题实战的答案从没被对账过。** 现在 = 通配找答案 + **通用兜底抽题**（保守三条判据）+ **覆盖账**（未覆盖必须有名有姓，否则算红）。
- **死指针 13 处**（学习规则.md / 语法速查.md）在**各课文件**里，其中 `../学习规则.md` 是**真断链** —— 已修（10 个文件）。**课次正文的教学内容一个字没动。**
- **★ 新查出的规则冲突**：`复习一` / `复习二` 的正文都让学生**自己写 `_清单.md`**，还要写**三行总结**；而 §5.2 规定清单**由教师提取**、三行总结 **2026-09-15 已取消**。→ 复习二（学生未做）**直接改正文**；复习一（已完成）**原地加更正框、原文留痕**；`复习二\code\_清单.md` 重写成**教师用骨架**。
- 根目录其余：`接手.md` §5/§7 空桩 → 显式「（已归档）」标题（**编号不动** —— 7 处引用 `§6` 才不会指错）；`模板.md` 两处 `§2`→`§6.3` + `语法速查` 3 处 + **环境路径去重**（只留 `约定-学生.md` 一份）；`进度.md` 悬空分隔线 + 死指针 + 两处 `§2`→`§6.3`；`约定-学生.md` 总表去掉**混合命名空间**行。

**改了什么文件**：`体检.ps1` · `对账.ps1` · `接手.md` · `学习规定-AI.md` · `约定-学生.md` · `进度.md` · `易错点台账.md` · `模板.md` · 本文件 · `第二课\`×2 · `第三课\`×1 · `第四课\`×2 · `第五课\`×1 · `第六课\`×1 · `复习一\`×2 · `复习二\`×2 · `真题实战\`×2

**验证**：
  - 命令：`.\体检.ps1` → `=== 结果：绿 === 通过 36 · 失败 0 · 警告 10`（改前 33/0/10，**且当时那 45 处缺陷一处都没被查到**）
  - 命令：`.\对账.ps1` → `=== 结果：绿 === 实跑与文档逐字一致 90 / 95`；`覆盖账：共 9 个文件夹 · 声明不覆盖 1 个 · 未覆盖 0 个 · 通用兜底抽到 2 题（另有 2 段含代码但不可自动对账）`
  - 命令：`[Parser]::ParseFile` 两份 `.ps1` → **0 个语法错误**；BOM 已确认在
  - 结果：**88/93 → 90/95**（新增覆盖的正是 `真题实战` 里能跑的那 2 段）

**⚠️ 一个必须说的坑**：两个 `.ps1` 的 **BOM 被我的编辑吃掉两次**（脚本自己警告过这件事）。
已用 `UTF8Encoding($true)` 补回并验证。**改 `.ps1` 之后必须重新确认 BOM。**

**没结的**：
- **「教学正文该不该有警戒线」仍无结论** —— 只加了"报体积"，阈值没定（`学习规定-AI.md` §9.1）
- **`真题实战\` 只能自动对账"能跑的那几段"**（2 题）；选择题 / 填空题 / 需 stdin 的综合题**靠人工**（记在 `进度.md` §6 待做 7）
- **复习一-正文要不要也直接改掉**（现在只加更正框、保留原文）—— 待学生定（`进度.md` §6 待议 6）
- **更正上一轮的一条错记**：那条会话写「根目录 `.md` **12 → 10 份**」，**实际是 10 → 8 份**（现在 8 份）

**下一个会话接手要点**：
- 开工先 `.\体检.ps1` + `.\对账.ps1` —— **两个都要绿**；红了先修
- **`规则N` 现在是合法的**（回 `学习规定-AI.md` 开头那张表查），**不要再去逐个改成 `§`**
- **改 `.ps1` 后必须确认 UTF-8 BOM**
- 下一步仍是：**等学生做第六课（16 题）+ 复习二（13 题）**

### 2026-09-22 · 会话：切割孤儿文档（`语法速查` 两份作废 + 台账改名精简）

**这轮读了**：`易错点台账.md`（**全读 538 行**——为压缩它，这是必要成本）
**体检**：跑了 —— **通过 33 · 失败 0 · 警告 10**（8 项检查）

**做了什么**：
- **`语法速查.md`（323 行）+ `语法速查-报错.md`（48 行）→ `旧文档\`**。判据是学生那句
  「**有现成的'老师'问，我没必要找讲解**」：它的两个声明用途**都被接管了**——
  "学生写代码时查" → 被"直接问 AI"接管；"教师查已学范围" → 被 `进度.md` §1 接管（§2.1 明写那是唯一依据）。
  **而且它和复习课的设计冲突**：§5.1 的全部意义是"提取练习（先不看资料）"，
  一份随手能翻的速查表正好在削弱它——**二级是闭卷笔试，考"想得起来"，不是"查得到"**。
- **把台账从「语法速查-易错点」改名为 `易错点台账.md`**（改名诚实：它是教师读的，不是"速查"），并加进 `体检` 的作废名单。
- **删掉 `体检` 检查 3（覆盖范围声明）**——它存在的唯一目的就是校验 `语法速查.md` 里那句覆盖声明。
  **文档死了，守着它的检查也得死**，否则它以后对着空气报 OK。9 项 → **8 项**，重编号。
- **台账精简 538 → 456 行**：条目 1（80 行，含庆祝段/原话/比喻）、13（两张表并一张）、15（长引文）、20（两段引文）压缩；
  **并删掉我自己今天加的那张「⚡ 速查 20 行」表**——它和 20 个标题完全重复，`grep '^## '` 就能拿到，
  **正是我当天批过的"一份信息存两处"**。

**又一次估错（今天第 4 次）**：我说台账"过于冗余"，**实测 20 条里只有 4 条臃肿，其余 16 条是 14~28 行的紧内容**。
我先试了"规则化搬迁叙述"（想省掉整读），**只搬走 16 行**——因为这里的叙述是普通 `>` 段落，没有可识别的关键词块。
最后只能真读真写。**教训：今天四次"我估计有 N 行冗余"全错，其中三次高估。估之前先数。**

**数字**：根目录活文档 **98,805 → 82,963**（−16%）· 热集 **26,419 → 25,550**（−3.3%）·
根目录 `.md` **12 → 10 份**。**热集只降 3.3%，说明台账本来就是紧的。**

### 2026-09-22 · 会话：给文档标「怎么读」（学生要求：分清全读/不全读）

**这轮读了**：`接手.md` §6 文档地图（~20 行）
**体检**：跑了 —— **通过 36 · 失败 0 · 警告 8**（检查 9 上线，共 9 项）

**做了什么**：
- **没有建"全读/不全读"两个文件夹**——那会让 `体检` 的**检查 5/6/7 全部静默失效**
  （它们只扫根目录；一搬家就打出**假绿**，热集字符数变 0）。这个理由写进了 检查 9 的注释里。
- **改成在 `接手.md` §6 文档地图里给每份文档标 `【怎么读】`**（不新建文件）。标出**五种**读法，不是两种：
  `全读一次`（接手）· `按节查`（学习规定/进度/约定-学生）· `先读速查表再 grep`（易错点）·
  `照抄骨架`（模板）· `不读/跑`（两个脚本、学生那三份、愿景）。
- **判据（学生问的就是这个）**：**全读 = 这份文档里"下一句话"依赖"上一句话"**。
  跳读会漏前提 → 全读（有顺序的清单）；每节能独立回答"什么时候查我" → 按节查（规则、条目、表格）。
- **新增 `体检` 检查 9**：**根目录每份 `.md` 都必须在文档地图里出现**（= 必须被标过怎么读）。
  防的是"加了文档但没人想过它该怎么读"。
- **检查 9 做了证伪试验**：塞一份 `_临时试验.md` → 它当场报出来；删掉 → 恢复绿。
  **不做这个试验就不算验过**（一个永远绿的检查比没有检查更糟）。

**代价（说实话）**：根目录活文档 **94,464 → 95,243 字符**——加标签表反而涨了 ~800 字符。
**这是花 token 买"以后不再新增没标读法的文档"**，不是省 token。

### 2026-09-22 · 会话：文档瘦身（删冷段，扔 旧文档）

**这轮读了**：`模板.md` / `接手.md` / `约定-学生.md` / `进度.md` / `学习规定-AI.md` 的**章节地图**（grep，没整读）+ 归档用的行段
**体检**：跑了 —— **通过 35 · 失败 0 · 警告 8**；**根目录活文档 98,805 → 93,406 字符**

**做了什么**：
- 搬进 `旧文档\文档瘦身-归档.md`（**272 行**）：`学习规定-AI.md` 的 §9.2/§9.3/§9.4/附·事件索引（−118 行，只留 §9.0 五条纪律 + §9.1 三条警戒线）· `模板.md` 开头 36 行漂移史 · `接手.md` 的「现在到哪了」一节（和 进度 重复）+ 「每课的循环」一节（和 §6.1 重复）（−28 行）· `约定-学生.md` 已取消的规则六（−17 行）
- `旧文档\AI 工作方法.md`（211 行，外来原文）—— **整份移出根目录**
- 修了 4 处因此产生的引用（备忘 2 处、`学习规定` §9.2 的 2 处历史引用）

**没做到的（重要）**：**我定的目标是 60,000 字符，实际只到 93,406。差得很远。**
原因不是没动手，是**我估错了**：我以为 `模板.md` 有 400 行冗余、`进度.md` 有 90 行冷段——
**`模板.md` 全是"写课时要照抄的骨架"（真在用）**；**`进度.md` 的 §4/§5/§7 全在每课收尾清单里（每课都要更新，不是冷段）**。
后者我一开始还错删了，自己查出来又还原。

> **结论（比这次瘦身本身值钱）**：这些文档**不是"装了很多不需要的东西"，而是"装了很多我用得着的东西"**。
> 真要砍到 60,000，就得砍掉**还在工作的内容**。所以——
> **唯一的"整份不需要"是 `旧文档\AI 工作方法.md`；其余的"不需要"其实是"我不该每次全读"**，
> 而那件事已经由 §9.0 的纪律（禁整读 / 先读速查表 / 按需 grep）解决了。
> **按停止条件：瘦身到此为止，不再开第二轮。**

**自己搞坏的一次（§1 铁律 3）**：搬运脚本给"同一文件的多个行段"用了**同一份快照**，
结果 §7 的改动被 §5 的改动覆盖（而且报告里照样打印"搬走 25 行"）——**批量改同一文件必须每次重读**。

### 2026-09-22 · 会话：压缩与省 token（学生：「token 也吃不消」）

**这轮读了**：`易错点台账.md` 顶部（**grep 定位后只读了 24~75 行**，没整读——按新纪律）+ `模板.md` §三 + `学习规定-AI.md` §9
**体检**：跑了 —— **通过 36 · 失败 0 · 警告 9**（`-短` 模式）；**对账 88/93**（静音模式）

**做了什么**：
- **★ 新增 §2.12：参考答案只给代码，不给讲解**（学生原话：「答案甚至只是一个形式，只要有正确的代码即可」
  「**这个文件没有正文重要，记住，没有正文重要**」「真有不懂的题，讲解我会找你的……有现成的'老师'问，我没必要找讲解」）。
  **§2.6 不取消**——"他错了说明什么"改成**在批改里说，不写进答案**。
  **已写好的第二~六课 / 复习一 / 复习二 不追改**（学生："不用改，以后记住就行"）
- **新增 §9.0：省 token 五条纪律** + **§9.0.1 不做整课重写、只做定点 `edit`**。
  其中最关键的一条认知：**外包（子模型）不省 token**——它读的也在同一账户上计费，只省我的上下文；
  **省 token 只有"少写、少读"两条**
- **`体检.ps1 -短`**（60 行 → **40 行**：跳过所有 `[OK]` 和逐文件体量表）+ **`对账.ps1` 默认静音**（100+ 行 → **16 行**）
- **`易错点台账.md` 顶部加 20 行「⚡ 速查」表**：每课先扫这 20 行，命中再 grep。
  **没有重写那 506 行**——我原以为"一大半是已解决的旧坑"，**grep 一查只有 1 条标了 ✅ 已解决**，
  所以改方案：**加表（25 行输出），不改内容（零风险）**
- **`模板.md` §三 的答案骨架删掉"讲解"段**，开头写明"哪题不懂直接问教师"

**改了什么文件**：`学习规定-AI.md`（§2.12 / §9.0 / §9.0.1）· `模板.md`（§三 骨架）· `易错点台账.md`（顶部速查表）· `体检.ps1`（-短）· `对账.ps1`（静音）· 本文件

**验证**：
```
命令： .\体检.ps1 -短   →  === 结果：绿 === 通过 36 · 失败 0 · 警告 9
命令： .\对账.ps1       →  === 结果：绿 === 实跑与文档逐字一致 88 / 93（另有 5 题「整理过」）
```

> **⛔ 我自己搞坏的一次（按 §1 铁律 3 主动写）**：给 `对账.ps1` 加静音时，我用"替换前缀"的方式改两行，
> **留下了孤立的 `}`** → 脚本直接 `Try statement is missing its Catch or Finally block`，跑不起来。
> **改完脚本必须真跑一次**——这次跑了才发现，没等下一轮。

**没结的**：
- ⚠️ **省 token 的效果还没被验证过**：下一课（第七课）是第一次按新纪律做，**该量一下实际花了多少**
- ⏸ 学生手上的第六课、复习二都还没做 → **两次批改都没发生**
- ⚠️ `易错点台账.md` 的 506 行**内容一行没动**，只是加了入口表——**真正的"降分辨率"还没做**

**下一个会话接手要点**：
- 出题/写答案**先读 §2.12 和 §9.0**（答案只给代码；禁整读；不整课重写）
- 第七课 字典与集合（第 6 级）——**第一课按新规矩走，顺便量 token**

### 2026-09-22 · 会话：复习二出题（13 题，**先出完题面再写答案**）

**这轮读了**：`学习规定-AI.md` §5.1（复习课五要点）/§6.0–6.1 · `复习一-正文.md`（结构 + 453 行）· `进度.md` §1/§2/§3/§6 · `复习一-参考答案.md`（对账用）
**体检**：跑了 —— 通过 **36** · 失败 **0** · 警告 **9**；**对账：85/90 一致**（5 题标了「（整理过）」）

**做了什么**：
- **`复习二-正文.md`（新）**：13 题 —— 甲 6（空白自测，**一题一课，六课全覆盖**）+ 乙 4（综合任务）+ 丙 3（读代码）+ 丁 收尾清单。
  照 §5.1 五要点：**累积（1~6 课）· 没一道原题 · 不限时 · 先不看资料 · 交清单**
- **`复习二-参考答案.md`（新）**：13 题的脚本 + 实跑输出 + 讲解 + 每题【他错了→说明什么】
- **`复习二\code\_清单.md`（新）**：清单骨架（想不起来 / 三行总结 / **已稳的** / 读代码感想）
- **★ 按学生新要求改了出题顺序**：**先把 13 道题面全写完，再一次性写答案**。
  已写进 `学习规定-AI.md` §6.1.5（含学生原话和"为什么这样还更对"）。
  **复习二的甲组输出一个都没进正文**——正文只有"运行示例"，答案全在参考答案里
- **`对账.ps1` 扩了两处**（不改就跑不了复习课）：① 断句边界认中文组号 `甲/乙/丙`；
  ② 扫描范围从"第X课"扩到 `复习N` / `真题实战`；③ 题号表加 `甲1~丙6`。
  扩完**复习一也顺带被纳入**（8/8 一致），**复习二 10/13 一致 + 3 题标「（整理过）」**
- 修 `复习二-正文.md` 甲6 的运行示例（`raw.strip()` 长度我写成 9，**实跑是 13**——就这一次没实跑，被抓住）

**改了什么文件**：**`复习二-正文.md`（新）** · **`复习二-参考答案.md`（新）** · **`复习二\code\_清单.md`（新）** · `进度.md`（课次表 + 待做）· `学习规定-AI.md`（新 §6.1.5）· `对账.ps1` · 本文件

**验证**：
```
命令： .\对账.ps1   →  复习二 一致 10 / 13（另 3 题「整理过」）；全库 85 / 90
命令： .\体检.ps1   →  === 结果：绿 === 通过 36 · 失败 0 · 警告 9
```

**没结的**：
- ⏸ 复习二 `code\` 是空的（学生没做）→ **批改（§4）没发生**，清单也还没提取
- ⚠️ **甲组的题面难度只是我的推断**，没有第二个视角看过（第六课那轮的教训：自证不够）
- ⚠️ 对账对**甲1/甲2/甲4** 是"跳过"（它们用 `input()`，交互式输出没法自动比）——
  **这三题的输出是我手写分行的，没进自动校验**。已用「（整理过）」标明，但**这是个已知的校验空档**

**下一个会话接手要点**：
- 学生做完复习二 → **按 §5.2 提取清单**（教师提取，不让学生自己写）→ 再走**真题实战二**（第 1~6 课）
- **出题记得分两遍**：先题面、后答案（§6.1.5）

### 2026-09-22 · 会话：第六课重写（两遍：我先出了一版，独立审核判"不能发"，再重出）

**这轮读了**：`学习规定-AI.md` §2（11 条判据）/§3/§6.1 · `进度.md` §1/§3/§6 · `易错点台账.md`（全 20 条）· `第六课-正文.md`（全 1470 行，改后逐块复核）· `第六课-参考答案.md` · `模板.md` §三/§六 · `备忘.md` 最新一条
**体检**：跑了 —— 通过 **30** · 失败 **0** · 警告 **9**

> ### ⚠️ 本轮最重要的一条：我第一版就又犯了 §2.2，是审核子模型抓出来的
>
> 我"重出"完 16 题、自己做完三方对账（13/13 逐字一致）之后，**以为可以发了**。
> 独立审核子模型的判定是：**"现在不能发"**——**C2 / D2 / F1 / H2 四道题，题面就是正文例题本身**：
>
> | 题 | 我犯的错（可复现） |
> |---|---|
> | C2 | ① 用的 `"xxaxx".strip("x")` **与 §3.3 陷阱框逐字同一串**，而正文**连结果 `'a'` 都印出来了** → "先猜再跑"被剧透 |
> | D2 | ② 用的 `"  a  b  "` **与 §4.1 例题同一串**，2 段/7 段的答案和结论都在正文逐字印着 |
> | F1 | 三行输出**与 §6.1 逐字相同**（连 `name`/`score`/`95.5` 都没换） |
> | H2 | ②③④ 的答案（`r"..."`、原因、`len` 对照表）**全在 §8.2 逐字印着** |
>
> **我自己后来又查出 H1 同病**：它的 `姓名`/`年龄`/`小明`/`18` 就是 §8.4 例题的内容。
>
> **教训（比这条 bug 本身值钱）**：
> **我做的"三方对账"证明的是"输出没抄错"，完全证明不了"题目没抄例题"**——
> 两件事用的是同一个我，**我只验了自己已经想到的那一类错**。
> `学习规定-AI.md` §1 说要"证据"，但**自证不是独立证据**。这一轮是子模型把这条补上的。
>
> **处理**：五道题全部重做（见下），不是打补丁。

**做了什么**：

**第一遍（重出旧题）**：
- **第六课出题整体重出（13 道常规 + 3 道特殊）**。前置审查结论：**讲课（§3）大体合规，出题（§2）严重违规**——逐字核过 A1、H1，**两道都是"抄例题"**（H1 连数据都不用改，它的【自检】就是正文示例输出的拼接）；按形状看其余 10 道同一毛病
- **`第六课-参考答案.md` 全篇重写**：每题**真跑**（Python 3.14.6 / `python -X utf8`），【自检】全是真实输出；每题带 **【他错了 → 说明什么】**，归因到易错点 ID
- **8 个新习题块回插 `第六课-正文.md`**：用 `@@A@@…@@H@@` 标记切成 8 段 + PowerShell **自下往上**替换旧区间（A 146–173 … H 1162–1196）。
  **替换前先断言**每段首行是 `## ★ X 组`、末行是 `---`，边界不符就抛错——**不靠"我记得行号"**

**第二遍（按审核结论重做 5 题 + 补 5 个空白知识点）**：
- **C2 / D2 / F1 / H2 / H1 五道题换数据、换目的**，每道都补上**新维度**（不是改数字）：
  | 题 | 新题面 | 新在哪 |
  |---|---|---|
  | **C2** | `cell = "**  重要  **"`，四行对比 `strip()` / `strip("* ")` / `replace` / 先挖后剥 | **第 1 行 `strip()` 一个字都没去掉**（两头是 `*` 不是空白）——**反直觉、且正文没印过** |
  | **D2** | `line = "name=张三;age=18;city=北京"` + `"a=b=c".split("=", 1)` | **`split` 三种形态一次考齐**，含 **maxsplit**（原题完全没有） |
  | **F1** | `data = ["张三", 18, "北京"]`：`{}` / **换序 `{2}{0}{1}`** / 只填两个字段 / f-string | **新组合**（换序+部分填充），不是复述 §6.1 那三行 |
  | **H2** | 三条路径 `bad` / `good1=r"..."` / `good2="..."` | **`\\` 写法**（原空白）+ **"源码写法 vs 字符串值"说准** + **`r"\"` 是 SyntaxError** |
  | **H1** | 表头换成 `商品`/`单价`/`苹果`/`5.5` | 不再和 §8.4 例题同数据 |
- **把 5 个"进度表标了 🔄 却一道题都没有"的知识点补进现有题**（不是加题，16 题不变）：
  `endswith` → **B1**；`replace` 第三参数 → **C1**；`split` 的 maxsplit → **D2**；
  `isalpha`/`isspace`/`isalnum` → **E1**（四个方法一次考齐）；`\\` → **H2**。
  并在 `进度.md` §2 第 4 级下面加了一张 **"题号 → 知识点 ID"对照表**，逐行可查
- **删掉 C1 答案里一条会误伤正确答案的诊断**（原文："顺序写反却结果一样 → 说明是背下来的"）。
  审核跑了 **6 种排列 × 7 个输入，结果全等** → 那是**等价正确答案**。
  **这正是第五课 F1 判分过严（2.0→3.5）的同款错误，我又犯了一次。**
- **E1 补上缺失的象限**：审核指出 5 个值只覆盖 `isdigit`/`int` 的 3 个象限，**缺"isdigit=True 但 int() 报错"**。
  实跑确认：`"²".isdigit()` → **True**，`int("²")` → **ValueError**。已加进题面（现在 **6 个值、两个方向都对不上**）
- **G1 把"不许用链式比较 `"a" <= c <= "z"`"从答案挪进题面**——原来只有答案里禁止，学生看不到，而那是最顺手的写法（实跑可用）
- **A1** 补判分口径（`capitalize()`/`replace()`/`title()` 也算对，因为它们都"造了新串"）+ 补第 4 问答案（原答案缺）
- **B1** 第 4 问措辞改成"打印出来是什么？为什么不是 `False`？"（`find` 给的是**位置**）
- **§3.4 链式调用**：删掉"顺序见下面 D1"（**指错了，D1 是 split/join**），改成用**另一个串**演示"为什么顺序有时有关系"
- **§8.2 例题**的路径串从 `C:\new\test` 换成 `D:\note\test`——**否则习题和例题共用同一个串**（同类问题）
- **附录"实测报错原文"逐条实跑复核**（3.14.6）：**11 条里 10 条逐字对上，1 条是错的**——
  `"abc".splits(",")` 我写的是 `...has no attribute 'splits'`，**3.14 实际还会补一句 `. Did you mean: 'split'?`**。
  两张表都标着"全部是实跑抓下来的原文"（§1），**却有一条是我凭印象重写的**。已按真实原文改正，并把答案附录补齐到与正文同 11 条
- 修 **`第六课-正文.md` 里一处指向作废文档的操作指令**：第 27 行 I3 的规则出处写的是 `约定-教师.md §四·读代码`（**已作废**）→ 改成 `学习规定-AI.md` §2.10
- 修 **`学习规定-AI.md` 的 §9.2 ③ 表格里 `易错点台账.md` = "§三"** —— 拆出去之后那个文件**没有 §三 这一节**（它的标题是"学生易错点台账"，条目是 1~20）。
  **是 `体检.ps1` 检查 6 抓到的**：我只在备忘里写了一句 `易错点台账.md §三`，**一分钟内报红**。顺手也改了 `体检.ps1` 检查 1 的标题串（同一处笔误）
- 修 `模板.md` 里第六课的【自检】计数（**25 → 20**，我数错了一次又改成 21，最后**按实测**定 20）

**给 `体检.ps1` 补的两个盲区**（都是本轮真被咬到才发现的）：- **检查 4 原先只扫 .md**：课次文件夹里的非 md 散件一个都看不见——子模型丢在 `第六课\` 的 4 个 `_audit_*.py`，它照样打 `[OK] 文件名全部符合规范`。已加"非 md 散件"告警（实测立刻抓到那 4 个）
- **检查 5 不认通配符**：我在备忘里写了一句 `*.md`，`Test-Path '*.md'` 被通配符匹配到，**打出了假的 `[OK] *.md 存在`**。已跳过含 `*` / `?` 的"引用"

> ⚠️ **同一轮踩了两次的坑**：改完 `体检.ps1` **必须重贴 UTF-8 BOM**（`edit` 会剥掉，PS 5.1 按 ANSI 读 → `Unexpected token`）。
> 第一次改完记得补了；**第二次（加通配符跳过）忘了补，跑出来一堆乱码报错**。已在文件头留说明。

**★★ 学生要求"优化工作流"后新加的两个常驻脚本**（`体检.ps1` 7 项 → **8 项**，另加 `对账.ps1`）：
- **`体检.ps1` 检查 8**：**习题串 ↔ 例题串**（把 §2.2 那半条"能机械查"的判据自动化）。
  实测**立刻抓出 6 道**：C2/D2/F1/H2/H1/E1/A1（含我第二遍改完**又踩进去的** A1 `banana`——
  §2 例题里正好有 `text = "banana"`，我换成 `lemon` 才干净）。
  **还有 `第五课-正文` 的 `hello world`**（那是已通过的旧课，只报警告不追改）。
- **`对账.ps1`**：抽答案每题脚本 → 真跑 → 和【自检】/实际输出逐字比 → **70/72 一致**（第一课无答案，跳过）。
  它顺带逼出一个**新约定「（整理过）」**：第二课 B2、第三课 B3 是**故意展示报错**、把 traceback 整理成一行的块，
  标 `（整理过）` 才不报红——**和 `体检.ps1` 那个「（拟）」是同一个套路**。
  两个脚本都登记进了 `接手.md` §6 文档地图 + `学习规定-AI.md` §6.0，并写清"什么时候跑"。

**改了什么文件**：`第六课-正文.md`（1470 → 1628 行）· `第六课-参考答案.md`（全篇重写）· `进度.md`（§2 加对照表）· `模板.md`（自检计数）· `学习规定-AI.md`（§9.2 ③ 笔误 + 批次 2 引用改指归档；该节 2026-09-22 已归档）· `体检.ps1`（检查 1 标题 / 检查 4 盲区 / 检查 5 通配符）· **`旧文档\备忘-归档.md`（新，批次 1~3 搬过去）** · 本文件

> **顺手做的一次归档**：本文件加了这一大条之后 **513 行，越过 500 警戒线**（`体检.ps1` 检查 7 报了）。
> 按 `学习规定-AI.md` §9 的表（"`备忘.md` > 500 行 → 归档早期批次"）把 **§二 的批次 1/2/3** 搬到 `旧文档\备忘-归档.md`（**内容一字未改**），
> 本文件留指针 → **513 → 438 行**。引用批次 2 的那一处活文档（`学习规定-AI.md` §2.2）已改指归档文件；
> **批次 4/5 留在原处**（引用多、还在被查）。

**验证**：
```
命令： python -X utf8 <每题脚本>  →  与【答案自检】和【正文自检】逐字比（13 道常规题）
输出： [OK] a1 b1 c1 c2 d1 d2 d3 e1 f1 g1 g2 h1 h2   13 / 13
结果： 13/13 三方一致（实跑 = 答案【自检】 = 正文【自检】）；体检 通过 30 · 失败 0 · 警告 9
另跑： I1 的 8 行格式说明符、I2 的坏代码/好代码、I3 的 bird/8/[]/IndexError、附录 11 条报错原文 —— 全部逐字对上
```

> **可以给下一轮当证据的一条**：`§2.2` 的判据（"**这题允许抄吗？把例题的数字改一改就能过 → 弱**"）
> **一击命中了我连续两课的同一个错**（第五课 A 组 + 第六课 12 题）。
> 它不是马后炮——是**先用判据查出旧题违规，再按判据重出**的。
> **但更值钱的一条是它的反例**：**我的"三方对账 13/13"全绿，题却仍然不能发**——
> 见开头那个 ⚠️。**能自己验的只有"输出的对错"，验不了"出题的好坏"。**

**没结的**：
- ⚠️ **难度曲线没有客观依据**：我按 §2 判据设计，但"这题对零基础算难吗"**没有证据**，只是推断
- ⏸ 第六课 `code\` **还是空的**——学生没做，**批改（§4）没发生**
- ⚠️ **§2.6 的"他错了说明什么"仍然只有一版**：审核只挑出 C1 那条不成立，**其余 15 条没有第二双眼睛看过**
- ⚠️ **审核子模型自己也只逐字核了 §8.2 和 H1/H2**，其余章节它**明说"没逐行读完"**（靠 grep 推断）——
  **这一轮的"审核通过"不是全量审核**
- 💡 **建议（没动手，等拍板）**：把"抽脚本→实跑→和两份文档的【自检】逐字比"收进项目当常驻工具（`体检.ps1` 加一项，或独立 `对账.ps1`（拟））。
  理由：**它抓的是我最容易犯的错**——【自检】写"记忆里的输出"而不是真跑的输出（`.splits` 那条就是这么漏的）。
  要动得同时登记进 `接手.md` §6 文档地图 + `学习规定-AI.md`，**所以没自作主张**
- 💡 **更该补的是另一件事**：`§2.2` 的判据（"这题和正文例题同不同串"）**目前只能靠人眼/子模型看**。
  可以考虑给 `体检.ps1` 加一项**机械检查**：把正文里 ```` ```python ```` 例题块里的字符串字面量抽出来，
  和习题块里的比一遍——**同串就报红**。本轮 5 道违规里有 4 道能被这一条直接抓出来（A1/H1/C2/D2/F1/H2 全部命中）。
- ⚠️ **`第六课\第六课-正文.md` 不在 `体检.ps1` 文档体量检查范围里**（检查 7 只扫根目录），它已经 **1628 行**、比任何一份根文档都长。**教学正文该不该有警戒线，没有结论**

**下一个会话接手要点**：
- 学生做完第六课 → 按 §4 批改归因 → **复习二（1–6 课）** → **真题实战二** → 第七课 字典与集合（**没有小项目**，规则六已取消）
- 出题前**逐题过一遍 §2.2**（判据在，就别再犯抄例题的错）
- **出完题要跑一次独立审核**——本轮证明了"自证不够"（见开头 ⚠️）

> ### 📦 §一 的早期会话条目已归档
>
> 搬到 `旧文档\备忘-归档.md`（2026-09-22 第三次归档；本文件越过 500 行警戒线）。
> **本文件只留最近 3 条会话 + 各批次的指针。**
# 二、决策记录（按讨论批次）

> **规律**：规则文档（`学习规定-AI.md` / `约定-学生.md`）写"是什么"，这里写"为什么"。
> **编号说明**：前三次讨论都在 2026-09-15，各自从 #1 重新开始，所以**单看数字有歧义**（有三个 #1）。
> 引用时必须带批次，如「批次 3 #22」。批次 5 起编号连续（#40~）。

> ### 📦 批次 1 / 2 / 3 / 4 / 5 **全部已归档**
>
> 都在 `旧文档\备忘-归档.md`（2026-09-22 分两次归档：本文件两次越过 500 行警戒线）。
> **§二 在本文件里已经没有了** —— 要查"这条规则当初为什么这么定"，直接开那份归档。

---


# 三、几件值得单独存档的经过

## 6.1 规则三的由来：第二课的病（出题人自己的责任）

**第二课的毛病是机械对应**：讲一个点 → 配一道题 → 下一个点 → 再配一道题。
结果是 20 道互不相干的小题，还互相重复（B2 / B3 / B5）。

**后果链**：重复的题 → 无聊 → 敷衍（硬编码一个答案交差）→ **被误判成"态度问题"**。

**结论（学生的话推出来的）**：
> **"简单不是不能有，主要是重复性。"**
> **"过度的重复有时候会令人疲惫。"**

**所以**：~~宁多勿难~~ → **宁准勿多。每点一题。**
**一道重复题的伤害不是浪费时间——是它会诱发敷衍，把"没练"伪装成"练了"。**
**责任在设计，不在做题的人。**

## 6.2 规则七的判分标准为什么改松（第五课 F1，2.0 → 3.5）

**教师初判**：F1 得 2.0/5。理由：没写代码、Q3 答错、Q5 没答、没贴文档原文。

**学生提出"判分太严格"——教师重读后，学生是对的。**

**学生原话**：
> "**还是保留**（我没说这种题型不行，而是**判分或者判错太过严格**，仅此而已，这种题型保留是没问题的。）"
>
> "这毕竟是**概念理解题**……大部分实际情况，`range` 和 `while` 的使用，很少会直接套在列表里或者直接打印。"

**教师当时把"输出的类似数组的简写"当成了含糊其辞**，其实它答对了：
`print(range(5))` 输出 `range(0, 5)`，**而 `range(0, 5)` 就是 `[0,1,2,3,4]` 的紧凑写法**。
**教师只认字面量，所以判错了。**

**改后的标准**：

| | 旧标准 | **新标准** |
|---|---|---|
| 看什么 | 交付齐不齐 | **理解对不对** |
| 强制写代码 | ✅ | ❌ 不强制 |
| 强制贴文档原文 | ✅ | ❌ 不强制 |
| 没答全 | 不给分 | **按理解程度部分给分** |

> **保留的软提醒（不扣分）**：Q3 那次是**想错了**，说明脑子里的模型有错——**这个靠刷题兜住。**

**并存档**：初判 2.0 的意见保留，**为了留痕**——以后回头看，能看见标准是怎么变的、为什么变。

## 6.3 学生发现出题人错误的五次（这是真正长出来的能力）

| # | 时间 | 学生发现了什么 |
|---|---|---|
| 1 | 第二课 | 教师正文里 `//` 的定义**写错了**（学生提问触发） |
| 2 | 第三课 c3 | 教师正文对**步长**的措辞**有歧义**（"每 2 个取 1 个"） |
| 3 | 第二课之后 | **出题重复**（B2/B3/B5 是同一个知识点的重复） |
| 4 | 真题实战一 | **卷子超纲**——操作题第 2 题用了第五课的 `for`，而卷子声明只覆盖第 1~4 课 |
| **5** | **真题实战一之后** | **批改的归因错了**（`bool("0")` 判成"概念没内化"，实为看漏引号） |

> **第四次的细节**：学生在卷子上直接写——
> `(此题超纲，建议按照0分算，1到4课没有讲循环)`
>
> **他没做那道题，但写下了理由**——这正是规则五（跳题给理由）要求的。
> **而卷子按他的要求原样保留、不改**（他说"超纲就超纲，这确实没法，但卷子设计没啥问题，保留就好"）。
>
> **这已经是第五次了。而这个能力——"验证出题人，而不是相信出题人"——才是真正在长出来的东西。**

## 6.4 规则十二（提交即冻结）的由来

**学生原话**：
> "不用了。作业留痕。我既然说了提交，那就说明我准备以后面对这个刺眼错误的决定，
> 以后我能看见这个错误——**只要我说了提交打分，就说明我这边做好了「冻结」的决定**，
> **你也没见过学生能后面改卷子分数的吧？**"

**教师的判断**：**这个类比是准的。事后改卷子 = 事后改分，是荒唐的。**

**配套调整**：既然提交后不能改，"检查"（`跑一遍`）就必须发生在**提交之前**。
第四课 `b4.py` 扣的 3 分，就是**按提交时的状态**算的——**这才是公平的位置。**

---

# 四、待议（需要双方确认才能变成约定）

| # | 事项 | 现状 |
|---|---|---|
| 1 | **规则三的字面表述**："同一知识点只考一次，**无论隔多久**"——和"遗忘"有冲突。真题实战一里 `/` 和 `//` 混了，正是隔久了模糊 | **未改**。目前的补丁是规则九（复习课）+ 规则十一（真题） |
| 2 | **`/` vs `//` 那次失分的性质** | 教师**没有问过学生**当时是怎么想的，直接在批改里归成"隔久了遗忘"。**按新的批改归因规则，应改记为"疑似符号看漏"，待问** |
| 3 | **阶梯排序**：第 8 级（明确"二级不考"）排在第 7、9 级（大纲主体）前面 | **未改** |
| 4 | **3 处命名偏差**（`体检.ps1` 检查 4 查出，2026-09-22） | `第二课-数字文本与f-string.md`（正文用了描述性名字）· `第一课-风格与习惯问答.md`（额外交档）· `复习一-批改.md`（应为 `复习一-作业批改.md`）。**要么改文件名，要么记进 `体检.ps1` 的例外清单——学生定** |
| 5 | **"终点线"（NumPy → 向量化 → PyTorch）只有一行字，没有计划** | 学生目标是"吃饭的东西"，而阶梯走到头只是"会写单文件脚本"。**建议以后补一段**（与 `愿景.md` 有关） |
