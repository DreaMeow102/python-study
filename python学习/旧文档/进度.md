# 进度（现在到哪了）

> **📖 指针** ｜ 本项目活文档见 `接手.md` §6 文档地图（**以那份为准，别抄这份的清单**）。
> `旧文档\` 一律作废，**不要以它为准**；`愿景.md` 是学生私人文件，**不参与阅读，教师勿动**。
>
> **本文档 = 现在到哪了**（阶梯 / 已考知识点 / 课次 / 真题 / 大纲对照）
> **不装**：为什么这么定 → `备忘.md`｜语法怎么写 → **无独立文档，直接问教师**（`语法速查.md` 2026-09-22 作废）｜格式 → `模板.md`

> **这份文档回答三个问题**：学了什么 · 考过什么 · 下一步是什么。
>
> **谁改它**：教师 AI。**每课批改后必须改**（清单见 `学习规定-AI.md` §6.3）。
> **它是 §2.7（不重复）唯一的执行依据**——出题前必须先过一遍 §2。
>
> **写法约定**：这里只写**当前值**。值是怎么变过来的、为什么变，写进 `备忘.md`。
>
> ---
>
> | | |
> |---|---|
> | **最后更新** | 2026-09-22（文档重构审查之后：修正 2 处事实错误 + 3 处记账缺口）<br>2026-09-22（**引用清扫**：死指针 / 错节号 / 遗留编号 —— **事实与分数未动**） |
> | **已覆盖** | 第 1 ~ 5 课 + 复习一 + 真题实战一 |
> | **下一课** | **第六课《字符串方法》——题目已出，学生尚未开始** |
>
> **这份文档已过期吗？** 对照 `进度.md` §3 课次表和实际文件夹。发现不一致 = 表错，以课次文件夹里的作业为准并回来修。

---

# §1 能力阶梯（14 级）

> **排序 = 教学顺序。** 每一级是一句"**学完你能……**"，这样**空着哪一格一眼看得见**。
> **状态**：✅ 已完成 · 🔄 已出题待做 · ⬜ 未开始

| # | 学完你能…… | 需要的东西 | 状态 | 二级考点 |
|---|---|---|---|---|
| **0** | 让程序跑起来、打印东西 | `print` / 注释 / 缩进 / 运行方式 | ✅ 第一课 | (一) |
| **1** | 读入数据并算出来 | `input` / `int` `float` `str` / 算术 / f-string | ✅ 第二课 | (二) |
| **2** | **根据情况做不同的事** | `if` / `elif` / `else` / `and` `or` `not` | ✅ 第四课 | (三) 分支 |
| **3** | **重复做同一件事** | `for` / `while` / `range` / `break` / `continue` | ✅ 第五课 | (三) 循环 |
| **4** | **切分和处理文本** | 字符串方法：`split` / `join` / `strip` / `replace` / `upper` / `find` | 🔄 **第六课（已出题 16 道）** | (二)(六) |
| **5** | 把一堆数据装起来、取一段 | 列表 / 索引 / 切片 / 常用方法 | ✅ 第三课 | (五) 列表 |
| **6** | 按名字存数据、去重、不可变 | 字典 / 集合 / 元组 | ⬜ | (五) 字典·集合 |
| **7** | 把代码拆开复用 | `def` / 参数 / 返回值 / 作用域 / 递归 | ⬜ | (四) |
| **8** | 一行写出"循环 + 筛选" | 推导式 | ⬜ | （二级不考） |
| **9** | 读写外部文件 | `open` / `with` / CSV | ⬜ | (六) |
| **10** | 让程序不崩 | `try` / `except` | ⬜ | (三) 异常 |
| **11** | 用标准库 | `import` / `random` / `time` / `turtle` | ⬜ | (七) |
| **12** | 装并用第三方库 | `pip install` / `jieba` / `PyInstaller` | ⬜ | (七) |
| **13** | 拆成多个文件、写成对象 | 模块 / `__main__` / 类 | ⬜ | （二级不考类） |

**当前：14 级里站稳 5 级（0、1、2、3、5）。只差第 4 级，第 1 段就收官。**

> **注意第 2、3、4 级的位置**——它们**夹在第 1 级和第 5 级中间**，而学生**跳着把第 5 级做了**。
> 所以现在能"装一堆数据、切一段"，**但还不能让程序做判断、不能重复、不能处理文本**（前两项第四、五课已补上）。
> **这个洞之所以很晚才被发现，就是因为旧路线图里根本没有这几格。**

**为什么字符串方法排在第 4 级（循环之后）**：`split` / `join` 在真实文本处理里**经常配合循环用**。

**为什么第三方库单独成一级**：二级明确要求"掌握不少于 2 个标准库 + 不少于 2 个第三方库"，还要"了解 `jieba`、`PyInstaller`，掌握用 `pip` 获取和安装第三方库的方法"。**这是一块独立的考点，原路线整块漏了。**

---

# §2 已考知识点清单 ★ 出题前必查

> **§2.7 的唯一执行依据。** 新题不得覆盖下表任何一行。
>
> **"已考" = 学生已完成；"已出题" = 题目已写、学生还没做。两种都算重复，都不许再出。**
>
> **ID 规则**：`级别.序号`，和 §1 的阶梯号对齐。
> **跨课复用的知识点不新开 ID**——复习课和真题换场景考，仍然记在原 ID 上（场景记在"考于"列）。
>
> **状态**：✅ 已考（过） · ⚠️ 已考（**答错**） · 🔄 已出题（未做）
>
> **⚠️ 的读法（2026-09-22 修正）**：⚠️ 的行**性质就地写在行内**；
> **只有真题实战相关的失分**才同时进 §4 的性质表。**不是所有 ⚠️ 都能在 §4 查到。**

## 第 0 级（第一课）

| ID | 知识点 | 考于 | 状态 |
|---|---|---|---|
| 0.1 | `print` | 第一课 | ✅ |
| 0.2 | 引号（含中文引号报错） | 第一课 | ✅ |
| 0.3 | 变量 | 第一课 | ✅ |
| 0.4 | 注释 | 第一课 | ✅ |
| 0.5 | 缩进 | 第一课 | ✅ |
| 0.6 | 运行方式 | 第一课 | ✅ |

## 第 1 级（第二课）

| ID | 知识点 | 考于 | 状态 |
|---|---|---|---|
| 1.1 | 数字类型（`int` / `float`） | 第二课 | ✅ |
| 1.2 | `/` vs `//` vs `%` vs `**` | 第二课 / 复习一（分装、对折）/ 真题一 | ⚠️ **真题一答错**（符号看漏） |
| 1.3 | `type()` | 第二课 | ✅ |
| 1.4 | **字符串 vs 数字的区别** | 第二课 | ✅（**第一课就自己改了，不要再考**） |
| 1.5 | `int()` / `float()` / `str()` 转换 | 第二课 / 复习一 / 真题一 | ✅（**易错点第 1 条，第四次才自己跨过去**） |
| 1.6 | f-string 基本用法 + `.2f` | 第二课 | ✅ |
| 1.7 | `input()` 返回字符串 | 第二课 | ✅ |
| 1.8 | `int(input())` 转换 | 第二课 | ✅ |
| 1.9 | 简单算术应用（时分秒、找零） | 第二课 | ✅ |
| 1.10 | **运算符优先级**（`**` 右结合；`*` 先于 `+`） | 真题一 | ⚠️ **答错**（概念缺口） |

## 第 2 级（第四课）

| ID | 知识点 | 考于 | 状态 |
|---|---|---|---|
| 2.1 | 六种比较运算符（`>` `<` `>=` `<=` `==` `!=`） | 第四课 | ✅ |
| 2.2 | `True` / `False` / **`None`** | 第四课 | ✅ |
| 2.3 | 真值判断（`bool()`；`0` `""` `[]` `None` 算假） | 第四课 / **真题一** | ⚠️ **真题一答错**（**性质：符号看漏**——`bool("0")` 看成 `bool(0)`，见 §4） |
| 2.4 | 单分支 `if`（冒号 + 缩进 + 归属） | 第四课 | ✅ |
| 2.5 | `if` / `else` | 第四课 | ✅ |
| 2.6 | `if` / `elif` / `else`（含**顺序**问题） | 第四课 | ✅ |
| 2.7 | `=` vs `==` | 第四课 b4 / 第五课 E1 | ⚠️ **第二次犯**（**性质：符号看漏 / 手快**——两次都是写完**立刻自己发现并注释掉**，不是不懂区别；治法见 `易错点台账.md` §19） |
| 2.8 | `and` / `or` / `not` | 第四课 | ✅ |
| 2.9 | 逻辑运算**优先级**（`not` > `and` > `or`） | 第四课 | ✅ |
| 2.10 | 嵌套 `if` | 第四课 | ✅ |
| 2.11 | 闰年判断（`and`/`or`/`%` 综合） | 第四课 | ✅ |
| 2.12 | 回文判断（切片 + `if` 综合） | 第四课 | ✅ |
| 2.13 | **`and`/`or` 返回操作数本身**（非布尔） ★I1 首次（§2.10） | 第四课 | ✅（第四课 e1 说反 → 真题一答对） |
| 2.14 | **改错题：`elif` 顺序错** ★I2 首次（§2.10） | 第四课 | ✅ |

## 第 3 级（第五课）

| ID | 知识点 | 考于 | 状态 |
|---|---|---|---|
| 3.1 | `for` / `range(n)`（含头不含尾） | 第五课 | ✅ |
| 3.2 | `range(a, b)` / `range(a, b, step)` | 第五课 | ✅ |
| 3.3 | 遍历列表、**遍历字符串** | 第五课 | ✅ |
| 3.4 | **累加器**（`total = total + x`） | 第五课 | ✅ |
| 3.5 | **计数器**（`cnt = cnt + 1`） | 第五课 | ✅ |
| 3.6 | **找最大·打擂法**（从 `nums[0]` 起打） | 第五课 | ✅（结论对，**理由当时是运气**，已澄清） |
| 3.7 | `for` + `if`：筛选 / 查找 | 第五课 | ✅ |
| 3.8 | `while` 基本结构 + **条件与起点配套**（`<` vs `<=`） | 第五课 | ✅（**自己发现并修好**） |
| 3.9 | **`while` 不知道要几次**（折纸类） | 第五课 | ✅ |
| 3.10 | `break` | 第五课 | ✅ |
| 3.11 | `continue` | 第五课 | ✅ |
| 3.12 | **`range()` 不吃字符串**（自己爬出来的坑） | 第五课 | ✅ |
| 3.13 | **读官方文档：`range` 对象** ★I1 第二次（§2.10） | 第五课 | ✅（判分标准已放宽） |
| 3.14 | **改错题：累加写成了替换** ★I2 第二次（§2.10） | 第五课 | ✅ |

## 第 4 级（第六课）—— 🔄 全部已出题，学生尚未开始

> **每题压在哪几行**（2026-09-22 第二次重出后核对，**每行至少有一道题真的用到**）：
>
> | 题 | 覆盖的知识点 ID |
> |---|---|
> | A1 | 4.1 / 4.2 |
> | B1 | 4.3 / **4.5**（`startswith` **和** `endswith` 都考） |
> | C1 | 4.6（**含第三个参数**）/ 4.7 / 4.9 |
> | C2 | 4.8 / 4.9 |
> | D1 | 4.10 / 4.11 |
> | D2 | **4.10 三种形态全考**（无参 / 指定分隔符 / 最多切几次） |
> | D3 | 4.12 |
> | E1 | **4.13 四个方法全考** |
> | F1 | 4.14 |
> | G1 | 4.15 |
> | G2 | 4.16 |
> | H1 | 4.19 / 4.20 |
> | H2 | **4.17（含 `\\`）** / 4.18 |
> | I1 | 4.21 | 
> | I2 | 4.8 / 4.2 + 真值判断 |
> | I3 | 4.10 / 4.11 + 第五课打擂法 |

| ID | 知识点 | 考于 | 状态 |
|---|---|---|---|
| 4.1 | 字符串**不可变**（能读、不能改） | 第六课 | 🔄 |
| 4.2 | 所有字符串方法**返回新串**（要接住） | 第六课 | 🔄 |
| 4.3 | `find`（找不到给 `-1`）/ `index`（找不到报错） | 第六课 | 🔄 |
| 4.4 | `count` | 第六课 | 🔄 |
| 4.5 | `startswith` / `endswith` | 第六课 | 🔄 |
| 4.6 | `replace` | 第六课 | 🔄 |
| 4.7 | `upper` / `lower` | 第六课 | 🔄 |
| 4.8 | `strip`（**只管首尾**） | 第六课 | 🔄 |
| 4.9 | 链式调用 | 第六课 | 🔄 |
| 4.10 | `split()` 三种形态（无参 / 指定分隔符 / 最多切几次） | 第六课 | 🔄 |
| 4.11 | `join`（方向与 `split` 相反） | 第六课 | 🔄 |
| 4.12 | 数字列表 → 字符串（循环累加 vs `join`） | 第六课 | 🔄 |
| 4.13 | `isdigit` / `isalpha` / `isspace` / `isalnum` | 第六课 | 🔄 |
| 4.14 | `format()`（含与 f-string 的关系） | 第六课 | 🔄 |
| 4.15 | `ord` / `chr` | 第六课 | 🔄 |
| 4.16 | `bin` / `oct` / `hex` | 第六课 | 🔄 |
| 4.17 | 转义字符 `\n` `\t` `\\` | 第六课 | 🔄 |
| 4.18 | 原始字符串 `r"..."` | 第六课 | 🔄 |
| 4.19 | 三引号 | 第六课 | 🔄 |
| 4.20 | 字符串乘法 `*` | 第六课 | 🔄 |
| 4.21 | **读官方文档：`format()` 的格式规格迷你语言**（`{:>8}` / `{:*^9}` / `{:08.2f}` / `{:,}` / `{:.1%}`）★I1 第三次（§2.10） | **第六课 I1** | 🔄 |
| 4.22 | **改错题：文本清洗（三处坑——`strip` 没接住 / 作用对象错 / 空字段没滤）**★I2 第三次（§2.10） | **第六课 I2** | 🔄 |
| 4.23 | **读代码：最长单词 + "全是空格"的隐藏坑** | **第六课 I3** | 🔄 |

## 第 5 级（第三课）

| ID | 知识点 | 考于 | 状态 |
|---|---|---|---|
| 5.1 | 创建列表 / `len()` / 混合类型 | 第三课 | ✅ |
| 5.2 | 正索引 / 负索引 / `IndexError` 越界 | 第三课 / 复习一（两层索引 `a[0][0]`） | ✅ |
| 5.3 | 切片 `[a:b]` / 省略写法 / 步长 / `[::-1]` 反转 | 第三课 / **复习一（用在字符串上）** | ✅ |
| 5.4 | 切片返回**新列表**（不影响原列表） | 第三课 | ✅ |
| 5.5 | 改元素 `a[i] = x` | 第三课 | ✅ |
| 5.6 | `append` / `insert` | 第三课 / 复习一 | ✅（名字忘过一次：`end`） |
| 5.7 | `remove` / `pop` / `del` 的区别 | 第三课 / 复习一（`remove(min(...))`） | ✅ |
| 5.8 | **方法 vs 函数**（`len(x)` vs `x.append()`） | 第三课 | ✅ |
| 5.9 | `sum` / `max` / `min` | 第三课 | ✅ |
| 5.10 | `sorted()` vs `.sort()`（新列表 vs 原地改） | 第三课 / 复习一 | ✅（方向不确定过） |
| 5.11 | `in` 成员判断 | 第三课 | ✅ |
| 5.12 | `b = a` 引用 vs `b = a[:]` 复制 | 第三课 / 复习一 | ✅ |

## 复习一（第 1~3 课，13 题）—— 迁移与全新场景

> **这些不是新知识点，是旧知识点换场景。** 记在这里是为了防止"同一场景再考一遍"。

| 涉及 ID | 迁移场景 | 状态 |
|---|---|---|
| 5.3 | 切片用在**字符串**上（第三课只对列表用过） | ✅ |
| 5.2 | 两层索引 `a[0][0]` | ✅ |
| 5.7 + 5.9 | `remove(min(...))` 组合用法 | ✅ |
| 1.2 | `//` 和 `%` 的新场景（**分装问题**，非"时分秒"） | ✅ |
| 1.2 | `**` 的新场景（对折翻倍） | ✅ |
| 0.1 | `print` 逗号版 vs f-string 空格对比（第一课那个坑） | ✅ |
| — | **读代码**：给代码不给输出，先读懂 | ✅ |

**成绩：92.5 / 100**（13 题里 12 题达标，**0 处实质概念错误**）

## 真题实战一（第 1~4 课）

| 涉及 ID | 情况 | 性质 |
|---|---|---|
| 2.3 | `bool("0")` **答错** | **符号看漏**（不是概念缺口，见 §4） |
| 1.2 | `/` vs `//` **答错**（答成 `3.5`） | 疑似**符号看漏**（待问，见 §4 备注） |
| 1.10 | `2**3**2` 答成 `64`；`"a"+"b"*2` 答成 `abab` | **概念缺口** |
| 2.13 | `5 and 0` → `0` **答对** | 第四课 e1 的坑修好了 ✅ |
| 1.5 | `x = int("24")` **答对** | 反复三次的 `float()` 坑真正过了 ✅ |
| — | 综合题第 5 行**缺 `是否全部及格：` 标签** | **交付不细** |

## 第 6 ~ 13 级

**未学。** 无已考知识点。

---

# §3 课次表

| 课次 | 主题 | 阶梯 | 状态 |
|---|---|---|---|
| 第一课 | 让程序跑起来 | 0 | ✅ 已通过（约 87） |
| 第二课 | 数字、文本与 f-string | 1 | ✅ 已通过（约 88） |
| 第三课 | 列表与切片 | 5 | ✅ 已通过（约 85） |
| **复习一** | 覆盖第 1~3 课 | — | ✅ **92.5 / 100** |
| **第四课** | **条件判断**（`if`/`elif`/`else`） | **2** | ✅ **92 / 100** |
| 第五课 | 循环（`for` / `while` / `range`） | 3 | ✅ **96 / 100** |
| 第六课 | **字符串方法**（全量：含格式化 / 编码 / 转义） | **4** | 🔄 **已出题（16 题，待做）** |
| **复习二** | 覆盖第 1~6 课 | — | 🔄 **已出题（13 题，待做）** |
| ~~小项目~~ | ~~（已取消）~~ | — | ⛔ **2026-09-22 学生取消**（理由见 `学习规定-AI.md` §5.6） |
| **真题实战二** | 第 1~6 课 | — | ⬜ 待做（**复习二之后**） |
| 第七课 | 字典与集合 | 6 | ⬜ 待写 |
| 第八课起 | 按 §1 的阶梯往下排 | 7~13 | ⬜ 待写 |

**顺序约定**：**复习课做完才进下一课。**

> **为什么第四课是 `if` 不是循环**：`if` 比循环更基础，而且 `for`/`while` 的循环体里**几乎一定要用 `if`**——先教 `if` 依赖关系更顺。
> **为什么字符串方法排到第六课**：它需要和循环配合才好用；而且**这是补课**——原路线整块漏了它。

### 粗坐标（旧分段，降级为参考）

| 段 | 内容 | 状态 |
|---|---|---|
| 第 0 段 | 跑起来（≈ 第 0 级） | ✅ |
| 第 1 段（重定义） | **第 1~5 级**：基础类型 + 控制流 + 组合数据类型 | 🔄 已完成 1、2、3、5；**只缺 4 → 第六课学完即收官** |
| 第 2 段 | **第 7、9、10 级**：函数 / 文件 / 异常 | ⬜ |
| 第 3 段 | **第 8、11、12、13 级**：推导式 / 标准库 / 第三方库 / 模块与类 | ⬜ |
| 终点线 | 进入 NumPy → 向量化 → PyTorch | ⬜ **目前只有一行字，没有计划** |

> ### ⚠️ 级别号已修正（2026-09-22）
>
> **原写的是**「第 2 段 = 第 **6、8、9** 级：函数 / 文件 / 异常」和「第 3 段 = 第 **7、10、11** 级：推导式 / 标准库 / 模块与类」。
> **对照 §1 的阶梯，只有「文件」和「标准库」两项对得上：**
>
> | 标签 | 原本挂的级别 | 实际级别 |
> |---|---|---|
> | 函数 | 6 | **7** |
> | 文件 | 9 ✅ | 9 |
> | 异常 | 8 | **10** |
> | 推导式 | 7 | **8** |
> | 标准库 | 11 ✅ | 11 |
> | 模块与类 | 11 | **13** |
>
> **→ 已按上表改为：第 2 段 = 7、9、10；第 3 段 = 8、11、12、13。**
>
> **顺带补一个孤儿**：**第 12 级（第三方库）原来在两段里都没出现**——
> 而它是**大纲(七)的独立考点**（`pip` / `jieba` / `PyInstaller`），当初就是为它单独加的一级。
> **一个级别不出现在任何"段"里，就等于没人会去排它。**

---

# §4 真题记录表

| # | 日期 | 范围 | 得分 | 主要失分点 |
|---|---|---|---|---|
| **一** | — | **第 1~4 课**（选择 30 + 操作 40 + 综合 20 = 90 分） | **61 分**（口径见下） | 见下面的性质表 |

> **卷子**：`真题实战\真题实战一-第1到4课.md`
> **答案**：`真题实战\真题实战一-参考答案.md`
> **批改**：`真题实战\真题实战一-批改.md`
>
> **⚠️ 这不是真题原文**——是按官方题型和分值出的仿真卷。

### 得分口径（两种，都记下来）

| 口径 | 算法 | 分数 |
|---|---|---|
| **A · 第 2 题作废**（教师采用） | 61 / **75** | **81.3%** |
| **B · 第 2 题按 0 分**（学生建议） | 61 / **90** | **67.8%** |

> **背景**：操作题第 2 题用了 `for` 循环——而循环是**第五课**内容，本卷只覆盖第 1~4 课。**超纲。**
> **是学生发现的**（他在卷子上写"此题超纲，建议按照 0 分算"）。
> **他的态度**："超纲就超纲，这确实没法，但卷子设计没啥问题，**保留就好**。" → 卷子原样保留，不改。

### ★ 失分点的性质（本次新增，见 `学习规定-AI.md` §4.1 / §4.2）

> **这条是把"答错"拆成三种，因为三种治法完全不同。**

| # | 缺口 | **性质** | 治法 |
|---|---|---|---|
| 1 | `bool("0")` 答错 | **符号看漏**（看成 `bool(0)`） | 练"读题时把引号念出来"。**不是复习真值判断** |
| 2 | `/` 和 `//` 模糊了（`7 // 2` 答成 `3.5`） | ✅ **已问。学生 2026-09-22 确认：「我当时确实忘了 `//` 和 `/` 的区别」** → **性质：隔久遗忘**（不是符号看漏） | **补知识 + 靠复习课 / 刷题兜**。⚠️ **这条同时是 §6 待议 #1「§2.7 和遗忘冲突」的第二个实证** |
| 3 | `2**3**2` 右结合；`"a"+"b"*2` 优先级 | **概念缺口** | 补知识：记两条（`**` 右结合；`*` 先于 `+`） |
| 4 | 综合题缺 `是否全部及格：` 标签 | **交付不细** | 提交前逐行对照题目给的输出样例 |

> **⚠️ 第 1 条的归因曾有误，已更正**：
> 原批改写的是"**真值判断没内化**"（= 提取失败 / 概念问题）。**学生指出真实原因是看漏引号。**
> 证据：同一张卷子第 7 题 `5 and 0` → `0` **他答对了**，说明"`0` 是假"他没问题；
> 而且他答的 `False 和 False` **正好是把他在第四课 a3 写的规律用在 `bool(0)` 上的结果**——
> **答案和他的规则自洽，只是喂进去的输入是看错的那个。**
> → 已按 §4.1 / §4.2 改为"符号看漏"，并把 `bool("0")` 的**性质**记进 §2 和 §3 的对应行。
> 详见 `旧文档\备忘-归档.md` 批次 4（#38）—— 2026-09-22 已从 `备忘.md` 归档。

> ### ★ 第 2 条的归因今天确认了（2026-09-22）
>
> **重构时把它从"隔久了遗忘"改成了"疑似符号看漏 ⚠️ 待问"**——因为按 §4.2，没问过就不能归因。
> **今天问了。学生的原话：「我当时确实忘了 `//` 和 `/` 的区别。」**
>
> | 时间 | 谁归的因 | 结论 | 对不对 |
> |---|---|---|---|
> | 旧批改（第五课前） | 教师 | "隔久了遗忘" | ✅ 结论对，**但是碰对的**——**没问就归因** |
> | 重构（2026-09-21） | 教师 | "疑似符号看漏" | ❌ **猜错了** |
> | **今天（2026-09-22）** | **问了学生** | **隔久遗忘** | ✅ |
>
> **这条的价值不在结论，在过程**：同一个错误，**只要不问，两次归因就错了一次半**（一次碰对、一次猜错）。
> **"归因前先问"不是礼貌，它是唯一能保证不归错的路径。**
> 完整记录见 `学习规定-AI.md` §4.2 + `旧文档\备忘-归档.md` 批次 5 专题。
>
> **⚠️ 由此浮出一个待议问题**：§4.1 的性质现在只有三类（概念缺口 / 符号看漏 / 交付不细），
> **"隔久遗忘"该塞进哪一类？**
>
> | 塞进"概念缺口" | 但治法对不上——概念缺口治法是"**补知识**"，而这里**知识后来长出来了、只是模糊了**，治法该是"**复习 / 刷题**" |
> |---|---|
>
> **治法不同 = 不该同一类** → **是否单列第四类「提取失败 / 隔久遗忘」**，留到逐文档那轮定（改 `学习规定-AI.md` / `约定-学生.md` 需双方确认）。

---

# §5 与计算机二级 Python 的对照

**Python 只在二级考**（一级是 Office，三级/四级不考 Python）。

### 分值结构

| 部分 | 分值 |
|---|---|
| 选择题 | **40 分**（含**公共基础 10 分**） |
| 操作题 | **60 分** = **简单编程 40 + 综合编程 20** |

> ⚠️ **及格不只是 60 分**：编程类科目**自 2019 年起增设选择题门槛**——
> **必须同时满足「总分 ≥ 60」且「选择题得分率 ≥ 50%」**（即 **选择题 ≥ 20 分**）才能拿证。
> **所以选择题不能放弃。**

### 操作题实际考什么（看过真题之后）

| 真题例子 | 用到什么 |
|---|---|
| "五个彩色圈圈" | **`turtle` + `random`** |
| "中文分词写文件" | **`jieba` + 字典词频 + 文件写入** |
| "价值链 · 词频 Top10" | **`jieba` + 字典 + `sorted(key=)` + 文件读** |
| "价值链 · 分句输出" | **`re` 正则 + `jieba` + 文件读写** |

**共同点**：
1. **核心是 `turtle` / `jieba` / 文件读写 / 字典词频统计**
2. **题型以「填空补全」为主**——给模板，填横线 / 改框错的代码，**不是从零写**
3. **几乎不考"纯逻辑题"**（闰年、水仙花数这类是**教材题**，不是真题形态）

> **结论**：**闰年不是"二级经典题"，是"教材经典题"。** 它仍然留着，是因为它练的 `%` + `and`/`or` + `if` 是上面所有大题的基础。

### 覆盖对照

| 二级大纲板块 | 对应阶梯 | 当前覆盖 |
|---|---|---|
| （一）基本语法元素 | 0 | ~75% |
| （二）基本数据类型 | 1、**4** | ~45%（**第六课补字符串方法**；还缺复数） |
| （三）程序的控制结构 | **2 ✅、3 ✅、10** | **~70%**（分支 ✅、循环 ✅，**只缺异常处理**） |
| （四）函数和代码复用 | 7 | **0%** |
| （五）组合数据类型 | **5 ✅**、6 | ~40%（列表 ✅，字典/集合/元组未学） |
| （六）文件和数据格式化 | **4**、9 | **~5%** |
| （七）Python 计算生态 | 11、**12** | ~5% |

**现在去考大约 40 分。**（分支和循环已会，但操作题主力——**字典/文件/`jieba`/`turtle`——全没学**。）

**走完第 0~12 级 ≈ 二级水平**（第 13 级是二级不考的类与模块）。

> ⚠️ **目标不是 60 分**（学生定的）："这个以后是我吃饭的东西，不只是过二级，**我二级也不是只要 60 分，我要能多高多高**。"
>
> | | 只想 60 分 | **要的：拿高分** |
> |---|---|---|
> | 操作题大块得分 | 够 | 够 |
> | **选择题的细节** | 可以放弃一些 | **必须全覆盖** |
> | "了解"级的内容 | 可略过 | **不能略** |
> | 阶梯能不能空格 | 能 | **一格都不能空** |
>
> > **二级是「地板」，不是「天花板」。**
> > 它不考 NumPy / PyTorch，所以**离 AI 方向还有一段**。

### 公共基础 10 分

真题选择题 40 分里有 **10 分是"公共基础"**（数据结构、算法、数据库、软件工程），**和 Python 无关，而且不在阶梯里**。
**处理**：等 Python 部分走完（约第 7~8 课后），再单独补一块。**现在不用管它。**

---

# §6 待做 / 已决定不做 / 故意延后

> **三种状态用三个标记**，别混——`⬜ 待做` 是欠着，`⛔ 不做` 是已拍板不做，`⏸ 延后` 是刻意推后。
> **混用会让下一个 AI 把"说好不做"当成"忘了做"。**

## ⬜ 待做

| # | 事项 | 何时 | 备注 |
|---|---|---|---|
| 1 | **第六课做题**（16 题，**题目已重出，等学生做**） | 现在 | 旧版 12 道常规题**违反 §2.2（抄例题）**，2026-09-22 已全部重出（正文+答案三方对账 13/13） |
| 2 | **复习二**（覆盖第 1~6 课） | **已出题，等学生做** | 13 题（甲 6 + 乙 4 + 丙 3）；甲组六课全覆盖 |
| 3 | ~~小项目~~ | — | ⛔ **2026-09-22 学生取消**（见 `学习规定-AI.md` §5.6） |
| 4 | **真题实战二**（第 1~6 课） | **复习二之后** | 小项目取消后，**不再有中间项** |
| 5 | 第七课 字典与集合 | 之后 | 第 6 级 |
| 6 | 公共基础 10 分 | 约第 7~8 课后 | 单独补一块 |
| 7 | **`真题实战\` 的答案只被对账覆盖了一部分** | 有空时 | 选择题只有字母、操作题是"填空"、综合题要 stdin —— `对账.ps1` 只能自动验**能跑的那几段**（现在 2 题）。**其余靠人工**。脚本已在汇总里报「覆盖账」，不会再假装全绿 |

## ⛔ 已决定不做

| # | 事项 | 何时定的 |
|---|---|---|
| 1 | **三行总结**（§5.7 ①） | 2026-09-15，学生："总结先不做了。大致先这样。" |

## ⏸ 故意延后

| # | 事项 | 延到哪 | 为什么 |
|---|---|---|---|
| 1 | **可变对象 / 引用语义的原理** | 后面正式讲 | 第三课只讲了 `b = a` 的**现象**，原理不提前 |
| 2 | 推导式（第 8 级） | 第 7 级之后 | 二级不考，而且必须先有 `for` |
| 3 | **把学习项目转化为产品**（网站 / 商业化） | **走完第 12 级（≈ 二级水平）之后** | 建站要用"跑完的语料"，不能边学边建。详见 `愿景.md` |

> **第 3 条不是学习任务**，是学生自己的长期备忘。**列在这里只是因为它的触发条件是"第 12 级之后"**——
> 到那时进度表会提醒他回去翻 `愿景.md`。**教师不需要为它做任何事。**

> ### ⚠️ 另一条已删除（2026-09-22）—— 它**从来不是延后**
>
> **原来的一行是**：「`format()` 格式说明符 | **第七课之后** | 第六课 §6.3 明确"这一节不讲"」。
> **（它和现在的第 3 条不是同一件事，只是当时占了第 3 行的位置。）**
>
> **但第六课 §6.3 说的是"换教法"，不是"往后放"：**
>
> | 第六课 §6.3 原文 | 指的是 |
> |---|---|
> | "**归谁讲**：你自己读官方文档（§2.10）" | **教法**（谁讲） |
> | "**在哪**：**I1 题**" | **位置**（就在第六课） |
>
> **而 §2 的 `4.21` 也正是这一条，本来就挂在第六课名下。**
>
> **两处直接矛盾**：§2 说第六课教、§6 说延后到第七课。
> **后果是实的**：下一个教师读 §6 会以为格式说明符**没教** → 要么**漏掉这个二级考点**，
> 要么以后重开一节（**违反 §2.7**）。**→ 该行删除。**

## ⚠️ 待议（需要双方确认才能动）

| # | 事项 | 现状 |
|---|---|---|
| 1 | **§2.7 的字面表述**："同一知识点只考一次，**无论隔多久**"——和"遗忘"有冲突。**2026-09-22 已有第二个实证**：`/` vs `//` 学生确认是"**确实忘了**"（不是符号看漏） | **未改**。见 `学习规定-AI.md` §5.3（张力说明）+ 本文件 §4 第 2 条 |
| 2 | 第四课/第五课批改文件名与命名规范不符（`-批改` vs `-作业批改`） | 已冻结，不追改；从第六课起按规范 |
| 3 | 阶梯排序：第 8 级（明确"二级不考"）排在第 7、9 级（大纲主体）前面 | **未改** |
| **4** | ★ **"读代码"在第四课、第五课连续两课落空** | **2026-09-22 新查出。补做还是认账不补，要学生定** |
| **5** | §4.1 的**性质只有三类**，"**隔久遗忘**"无处安放（治法与"概念缺口"不同） | **2026-09-22 新查出**。是否单列第四类，待定 |

> ### ★ 第 4 条的证据（2026-09-22 新查出）
>
> **规矩是明确的**：
>
> | 出处 | 原文 |
> |---|---|
> | `学习规定-AI.md` §2.10 | "**从第四课起，每课至少一题'读代码'**" |
> | `模板.md` 自查清单 | "□ 本课有 I1（读文档）/ I2（改错）/ **I3（读代码）** 吗？（第四课起）" |
>
> **实测全仓库**：
>
> | 出现位置 | 情况 |
> |---|---|
> | 复习一 | 丙1 / 丙2 / 丙3 —— **3 题** |
> | 第六课 | I3 —— **1 题** |
> | **第四课** | **0** ❌ |
> | **第五课** | **0** ❌ |
>
> **→ 连续两课落空，而且没进任何欠账清单。**
> **`模板.md` 开头那张"漂移表"只列了 2 项（批改文件名、【自检】标签），漏了这一项。**
> **这正是 `学习规定-AI.md` §2 收尾清单想堵的那类洞：规则落空了，但没人记账。**

---

# §7 分数趋势

| 课 | 分数 |
|---|---|
| 第一课 | 87 |
| 第二课 | 88 |
| 第三课 | 85 |
| 复习一 | 92.5 |
| 第四课 | 92 |
| 第五课 | **96** |
| 真题实战一 | **61 / 75 = 81.3%** |

> **⚠️ 分数不是主信号**：判分标准中途改过（第五课 F1 从 2.0 上调到 3.5；I1 的判分标准放宽，见 §2.10），
> 所以**前四课和后两课的分数不完全可比**。真题记录还留了两个分母。
>
> **比分数可靠的信号是这个**：
>
> | 课 | 自己修好的坑 |
> |---|---|
> | 第四课 | `float()` 返回值（第 4 次，自己跨过去） |
> | 第五课 | `<=` 的边界（自己发现并修好，旧版本注释保留） |
>
> **两次都是同一个模式：出过错 → 想清楚 → 自己改掉。这比一次做对更值钱。**
>
> **真正的主信号是 §1 的阶梯——"这一级过没过"，格子在不在。**
