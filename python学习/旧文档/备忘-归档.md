# 备忘·归档（旧决策批次）> **从 `备忘.md` 归档过来的**（2026-09-22，`备忘.md` 越过 500 行警戒线，按 `学习规定-AI.md` §9 归档早期批次）。> **内容一个字没改**，只是搬了个地方——引用这些批次的活文档已改成指向本文件。---## 批次 3 · 复习课 + 学习模式（2026-09-15）

| # | 议题 | 结论 | 谁提的 |
|---|---|---|---|
| 1 | 复习课频率 | **每三课一次**（第 3、6、9 课后） | 学生 |
| 2 | 复习课范围 | **累积式**（复习一覆盖 1~3 课，复习二覆盖 1~6 课） | 我 |
| 3 | 复习课形式 | **全新场景，没有一道原题** | 我 |
| 4 | 限时吗 | **不限时**（我原方案限时，**学生否了**） | 学生 |
| 5 | 看资料吗 | **先不看**，卡超 10 分钟可查，**但必须记清单** | 我 + 学生修正 |
| 6 | 复习必交 | **「想不起来」清单 + 三行总结**（后者后改为不做，见 #25） | 我 |
| 7 | 每课收尾 | **写三行总结；卡住时记「卡的过程」**（规则十） | 我 |
| 8 | 加"读代码" | **加**。从第四课起每课至少一题 | 我 |
| 9 | 语法速查 | **建根目录 `语法速查.md`**，只收录已学内容；**学生的易错点单独成节** | **学生提 + 我设计** |
| 10 | 根目录约定 | 从"只有 `学习规则.md`"**改成两个文档** | 学生 |
| 11 | `code\` 分子文件夹 | **不分子文件夹**，接受现状 | 学生 |
| 12 | 第四课时机 | **复习一做完再进** | 学生 |
| **13** | **复习一覆盖是否均衡** | **初版偏第三课（10 题里 7 题）**，学生发现后**补了第一、二课**（甲2/甲3/甲4）→ **13 题** | **学生提问 + 我修正** |
| **14** | **清单由谁写** | **改成教师从学生的代码注释里提取**（学生自己写会忘，且容易"感动自己"） | **学生** |
| **15** | **清单的格式** | **直接写正确知识点，不写"翻第X课第X节"** | **学生** |
| **16** | **三行总结** | **暂缓**，等学生拍板；且学生建议改成"师生一起生成" | **学生** |
| **17** | **第四课顺序调整** | 原计划"循环" → 改成 **"条件判断（if）"**，循环顺延到第五课 | 我（学生提问触发） |
| **18** | **复习一成绩** | **92.5 / 100**，13 题里 12 题达标，**0 处实质概念错误** | 我 |
| **19** | **前三课事后估算** | 第一课 87 / 第二课 88 / 第三课 85（第二课若扣掉我出题重复的责任约 92） | 学生要求 |
| **20** | **路线图改成「能力阶梯」** | **采纳。** 12 级，每级一句"学完你能……"。原知识点清单漏掉 `if`/`for` 就是因为它们**连格子都没有** | **学生**（我提议） |
| **21** | **第四课 = 条件判断** | 确认。`if` 先于循环，因为**循环体里几乎一定要用 `if`** | 我（学生提问触发） |
| **22** | **第 1 段重定义** | 原定义"数字与文本、list/dict/tuple/set、切片、**推导式**"**自相矛盾**（推导式必须先用 `for`）。现改为覆盖阶梯 **1~5 级** | 我 |
| **23** | **补两块原路线没有的** | **字符串方法**（`split`/`join`/`strip`…）和 **第三方库常识**（`jieba`/`PyInstaller`/`pip`）——二级考点，原路线漏了 | 我（对照二级大纲发现） |
| **24** | **考试定位** | Python 只在**二级**考。现在约 **25~35 分**（60 合格）；**走完第 0~12 级 ≈ 二级水平** | 学生提问 |
| **25** | **三行总结** | **不做**（"总结先不做了"）。规则保留，以后想加随时加 | **学生** |
| **26** | **补两块 + 阶梯扩到 14 级** | 新增 **第 4 级 字符串方法**、**第 12 级 第三方库**——对照二级大纲发现原路线整块漏了。**第六课 = 字符串方法** | **学生要求 + 我执行** |
| **27** | **目标定位** | **近期目标 = 过二级**（可测量、有期限、强迫覆盖）。**但二级是地板不是天花板**——不考 NumPy/PyTorch，离 AI 还有一段 | **学生** |
| **28** | **目标提高：不是 60 分** | 学生定：**"我二级也不是只要 60 分，我要能多高多高"**。→ 要求**选择题细节全覆盖、"了解"级内容也不能略、阶梯一格都不能空** | **学生** |
| **29** | **真题实战写成规则** | **规则十一：每 2 课一次真题；第 7 课后做完整限时卷。** 并新增真题记录表 | **学生** |
| **30** | **长期定位** | 学生原话：**"这个以后是我吃饭的东西"** → Python 是**职业工具**，二级只是**中途检查点** | **学生** |
| **31** | **第四课成绩** | **92 / 100**。亮点：`float()` 坑自己解决（第 4 次）、**a3 推翻教师的真值表且更优**、d2 独立推导出第二种闰年实现 | 我 |
| **32** | **规则七第一次执行** | E1 读文档：**Q4 说反了、且没贴文档原文**。暴露"倾向问我而不是读文档"。**结论：规则七有效，但仍需练** | 我 |
| **33** | **规则八第一次执行** | E2 改错题：**完全正确**（找到 `elif` 顺序错 + 补 `else`） | 我 |
| **34** | **必须纠正的自我标签** | 学生第二次说"我算法不行/数学很差"。**已当面纠正**：那是"急着下笔 + 需要多检查"，不是天赋问题 | 我（学生提出） |

---

## 批次 2 · 出题策略（2026-09-15）

> **这一批的核心是被否掉的提案**——记下来，免得以后又绕回去。

| # | 议题 | 结论 | 谁提的 |
|---|---|---|---|
| 1 | 用 C# 类比压缩路线 | **否掉**（"计算机语言在我眼里是不同的东西，我该重新学"） | **学生** |
| 2 | 知识点覆盖 | **全部覆盖，一个不跳** | **学生** |
| 3 | 重复出题 | **每个知识点只考一次**（规则三） | **学生** |
| 4 | 以留痕为准 | 只决定"讲多细"，**不决定考不考**（规则四） | **学生** |
| 5 | 跳题 | **可以跳，但要给理由**（规则五） | 我 |
| 6 | 小项目 | 第 1 段结束后插一个"没有答案"的经历（规则六） | 我 |
| 7 | 读官方文档 | 第四课起，每课一个知识点学生自学（规则七） | 我 |
| 8 | 改错题 | 第四课起，每课一道（规则八） | 我 |

### 专题：C# 类比提案被否的完整经过（#1）

**教师曾提议**：既然学生有 Unity/C# 经验，就用类比压缩路线、跳过"概念相同"的知识点，
甚至先做一张「Python ↔ C# 对照表」代替基础课。

**学生否了，而且理由更硬**：
> "这就像是你会中文和会英文的区别，**计算机语言在我眼里是不同的东西**，我认为我就该重新学。"
>
> "**超纲等于提前弄，提前弄会降低知识度。**"

**结论**：**会 `$"{x:F2}"` 不等于会 f-string。** 知识要在**新语言里重新长一遍**。
**这一条不许再提。**（`约定.md` 附 E 记了"该怎么用"，但那是两个用法，不是捷径。）

---

## 批次 1 · 基建（2026-09-15）

| # | 议题 | 结论 |
|---|---|---|
| 1 | 学习平台 | Windows 本机（不上 WSL/VM） |
| 2 | 目录结构 | 每课一个文件夹，根目录只放规则文档 |
| 3 | Git | GitHub 私有仓库 + SSH 密钥 + 代理 |
| 4 | 密码 | **绝不索取、绝不保存**；只用 SSH |

---

---

> **2026-09-22 第二次归档**：备忘.md 又过 500 行，把**批次 4 / 5**（含两个专题）也搬到这里。内容一字未改。

## 批次 5 · 文档体系重构（2026-09-22，承接第四次讨论之后）

> **起因**：第五课批改后发现三笔欠账（`语法速查` 漏了整块循环、已考知识点清单漏了第五课和真题实战一的行、
> 课次表与规则九冲突）。查根因时发现：**不是设计差，是"批改完就以为收尾了"。**
> 于是把 `学习规则.md`（1112 行，一份文件干四件事）拆开重做。

| # | 议题 | 结论 | 谁提的 |
|---|---|---|---|
| **40** | **文档划分原则** | 只按一条线切：**"每课谁写它、多久变一次"**。不按话题切、不按读者切 | 我提议 + 学生拍板 |
| **41** | **拆成 4 份活文档** | `约定`（极少改）· `进度`（每课改）· `语法速查`（每课追加）· `备忘`（只追加） | 我 + 学生 |
| **42** | **手册并入约定** | 原《学习规则》§四目录约定 / §七环境 / §八 git → 变成 `约定.md` 的**附录 A/B/C**。理由：两者都是"极少改"，靠话题分边界必然漂移 | 我提议 + 学生同意 |
| **43** | **新增 `模板.md`** | 正文 / 参考答案 / 批改 / 清单 的**格式**写死 | 我提议 + 学生同意 |
| **44** | **新增 `接手.md`** | 给"零上下文"的新教师 AI 的一页入口 | 学生提 |
| **45** | **新增「每课收尾清单」** | 写进 `约定.md` 附 D。**这是补批次 4 #37 那三笔欠账的机制** | 我 + 学生 |
| **46** | **知识点编号（ID）** | 格式 `级别.序号`，和阶梯号对齐。`进度.md` 出 ID，`语法速查` 引用 ID → **两份互为校验** | 我提议 + 学生同意 |
| **47** | **「正文与理由分离」** | `约定` 只留一句理由，长理由/原话进 `备忘` | 我提议 + 学生同意 |
| **48** | **规则四·补充：批改归因规则** | 归因"概念没掌握"之前必须先问"当时你怎么想的"；失分点分三类（概念缺口 / 符号看漏 / 交付不细） | **学生触发**（见下面专题） |
| **49** | **旧文档处理** | 两个旧文件移进 `旧文档\`，**仅历史参考，不要以它为准** | 学生指示 |
| **50** | **会话流水（本文件 §一）** | 每个会话必写一条。好处：**作"墙"触发器**——要写条目就必须先读上一条，把"读"绑回开工时 | **学生提** |
| **51** | **`体检.ps1`** | 学习项目没有测试套件，但**文档一致性可以跑**。4 项检查 + 退出码，作为出题前的第 0 步 | 我提议 + 学生同意 |
| **52** | **`流水.md` 升级为 `备忘.md`（没新建第 8 份）** | 会话和决策**都是"只追加"**，变化源相同 → 同一份。分两份会出现"这轮的决定算决策还是会话"的模糊边界 | 我判断（学生提会话流水的需求） |
| **53** | **`愿景.md`** | 学生私人备忘，记录"这套学习项目以后可能变成什么"。**几乎不改，教师不要动** | **学生** |
| **54** | **重写 `学习规定-AI.md`** | 旧版（484 行）是**流程约束集**，新版（379 行）加上了**教学法**：出题 / 讲课 / 批改归因 / 复习真题。**写法约定：只写有事件支撑的规则**，每条 = 动作/判据 + 【事件】+【自查】 | **学生要求重写** |
| **55** | **`体检.ps1` 扩到 6 项** | 新增**交叉引用有效性**、**节引用有效性**。理由：这两类是文档事故的主因（死引用、节号漂移），而且**都是本会话自己犯出来的** | 我 |
| **56** | **「已交付给学生的文件不追改名」** | 3 处命名偏差（第四/五课·复习一批改名、第一/二课正文名、第一课问答档）**收为例外**，原则写进 `模板.md` §一。**理由：学生已按旧名读过，改名 = 他找不到** | 我提议 + 学生拍板 |
| **57** | **§8 认账"没有判据的地方"** | 讲得清不清楚 / 他掌握到什么程度 / 课与课节奏 / 题难不难 —— **明写"没有判据，靠现场判断"**，不用漂亮话填 | 我提议 + 学生同意 |

### 专题：`bool("0")` 的归因更正（#48 的由来）

> **这是本次重构最重要的副产品：教师把一个错误归错了因，学生纠正了它。**

**教师的原始批改**（`真题实战一-批改.md` §三 / §八）：
> "你答 C（`False 和 False`），正确是 A…… **真值判断没内化**。
> 而你自己在第四课 a3 里写过这样一句话…… **你自己的规律能推出正确答案——但你没用上。**"
> → 归因为**提取失败 / 概念问题**。

**学生的更正（原话）**：
> "至于那个 bool 的错误……因为我是**下意识看到的 0 没注意引号**，粗心导致的，
> 我能承认这次错误，**但我认为我的知识总结没啥问题**。"
>
> "**恰恰是因为知识总结没啥问题**，0 本身输出就是 false，但字符串输出是 true——
> **这也是人才会犯的错误，AI 很注意双引号在不在。**"

**为什么学生的解释成立**（教师复核后的证据）：

| 证据 | 说明 |
|---|---|
| 同一张卷子第 7 题 `5 and 0` → **答对** | 说明"`0` 是假"他没问题 |
| 他答的 `False 和 False` | **正好是把他在 a3 写的规律用在 `bool(0)` 上的结果**——答案和他的规则自洽，只是输入看错了 |
| 若是概念没掌握 | 更可能答成 `True 和 False`（把"非空字符串都是真"搞反）——他没搞反 |

**结论**：**教师的归因是错的，已改。** 补救不是"复习真值判断"，是**"读题时把引号念出来"**。

**学生的这句话值得单独存档**：
> **"恰恰因为知识总结没问题，才会犯这个错。"**
>
> ——**规则越准确、越通用，套错输入的时候答案就越一致地错。**
> 而 AI 不会犯这种错（它不会漏看一个引号），**所以这类错误只能靠人自己的检查动作兜住。**

**这是学生第 5 次纠正教师**（前四次见 §三 6.3）。

---

## 批次 4 · 第六课设计 + 结账（2026-09-21）

> **起因**：教师问"要不要开始写第六课"，学生回"**你打算怎么做呢？**"——
> 于是先摆方案给学生审，再动手。**这次讨论有个副产品：核文档时查出三笔没结的账。**

| # | 议题 | 结论 | 谁提的 |
|---|---|---|---|
| **35** | **第六课范围** | **全量**：字符串方法（`split`/`join`/`strip`/`replace`/`find`…）**+ 选择题细节**（`format()` 格式说明符 / `ord` `chr` / `bin` `oct` `hex` / 转义 / 原始串 / `*` 重复）。学生的理由：**要高分，选择题细节不能漏** | 我提议 + 学生拍板 |
| **36** | **第六课之后的顺序** | **复习二 → 小项目 → 真题实战二** | 我提议 + 学生拍板 |
| **37** | **复习二时机（规则冲突）** | 以**规则九**为准：**第 6 课后、覆盖第 1~6 课**。**§5.4 课次表原来写「第 7 课后 / 覆盖 1~7」是错的，已改。** | 我查出 + 学生同意补 |
| **38** | **三笔欠账** | **现在就补**（不等第六课）：① `语法速查.md` 补第五课整块循环内容 ② §3.3 补第五课 + 真题实战一的知识点行 ③ 修正 §5.4 与规则九的冲突 | 我查出 + 学生拍板 |
| **39** | **第六课的定位** | 第 4 级。**二级操作题主力（`jieba` 词频 / 文件读写）全部踩在这一课上**——第五课给了循环，第六课给循环真正要加工的材料 | 我 |

### ★ 这次讨论暴露的系统性问题（本次重构的直接原因）

> **记下来，以后每课结束都要自查**：

| 欠的东西 | 谁负责补 | 什么时候补 |
|---|---|---|
| `语法速查.md` 的新课内容 | 教师 | **每课批改后** |
| 已考知识点清单 | 教师 | **每课批改后**（规则三的执行依据） |
| 阶梯状态 / 覆盖对照表 | 教师 | **每课批改后** |

**第五课这三样全漏了。**
**根因**：把"批改 + 打分"当成了收尾，**漏掉了批改之后的文档同步**。

> **当时的承诺是"从第六课起，文档同步写进收尾清单"。**
> **但那张"收尾清单"当时没有家——它是一句承诺，不是一个东西。**
> **批次 5 把它落成了 `约定.md` 附 D。**

---

> ### 📦 批次 1 / 2 / 3 **已归档**
>
> 搬到 `旧文档\备忘-归档.md`（2026-09-22，本文件越过 500 行警戒线）。
> **批次 4 / 5 还在下面**——那两批引用多、还在被查。

---

---

# 一、会话流水（早期条目·第三次归档 2026-09-22）

### 2026-09-22 · 会话：修 bug + 拆 语法速查（852 → 326/493/30）

**这轮读了**：`学习规定-AI.md`（全 605 行）· `进度.md` §1/§2/§3 · `语法速查.md`（全 852 行）· `语法速查 §三` 20 条易错点 · `接手.md` · `体检.ps1`（检查 5/7 源码）· `备忘.md` 最新一条 · `第六课-正文.md`（结构 + A1/H1）
**体检**：跑了 —— 结果：通过 **30** · 失败 **0** · 警告 **9**（**中途红过一次，见"自己搞坏的"**）

**做了什么**：
- **修 `体检.ps1` 一个真 bug**：`检查 7` 读文件漏 `-Encoding UTF8` → PS 5.1 按 ANSI 解，**中文的 UTF-8 字节把紧跟的换行吃掉**，行数少算 **16~28%** ｜ **修完才发现 `语法速查.md` 早就 852 行、越过 800 警戒线，而这个 bug 让它一直报 [OK]**
- **修 `接手.md` 6 处死引用**：旧的 `规则一/四/九/十二` 编号（已废）+ 两处把收尾清单指到 `§2`（**实际在 §6.3**）
- **修 `学习规定-AI.md §9.4`** 的快照数字（那是上面那个 bug 的产物）
- **按 §9.2 ③ 把 `语法速查.md`（852 行）拆成三份**：`语法速查.md`（〇+一+四，326 行）· `易错点台账.md`（§三，493 行）· `语法速查-报错.md`（§二，30 行）。**逐行搬运，一个字没改**
- **补上 §9.2 ③ 漏掉的同步项**：`体检.ps1` 检查 1（`【归属】`改读易错点）+ 检查 7（热集名单）—— 不改就断
- 把 5 份文档顶部那个"活文档 7 份"清单**换成一行指针** → 消掉一处 5 重副本
- **★ 学生取消了「小项目」（规则六）** —— 原话：「小项目我觉得可以取消了（实际上**影响代码学习**，我有项目经验，所以**不能让项目先阻挡我学习**）」「**但是复习和真题考试不行**」。
  已落盘：`学习规定-AI.md` §5.6（标 ⛔ 取消 + 留档原规则）+ `进度.md` §3 课次表 / §6 待做。
  **排期变成：复习二 → 真题实战二**（不再有中间项）

**改了什么文件**：`体检.ps1` · `接手.md` · `学习规定-AI.md` · `语法速查.md`（内容重写）· **`易错点台账.md`（新）** · **`语法速查-报错.md`（新）** · `进度.md` · `模板.md` · `约定-学生.md` · 本文件

**验证**：
```
命令： & "D:\studydemo\python学习\体检.ps1"
输出： === 结果：绿 === 通过 30 · 失败 0 · 警告 9
结果： 30 通过 / 0 失败。热集 25,746 字符（拆前 36,316）；最长单文件从 852 行降到 605 行
```

> **自己搞坏的（按 §1 铁律 3，主动写）**：拆完后我顺手把 §9.2 ③ 里那个「（拟）」标记删了，
> 而 `体检.ps1` 的约定是「**反引号 = 必须存在的引用，计划中的要加「（拟）」**」——
> 于是 `语法速查-速查.md`（拟）被当成一个真引用，**检查 5 一分钟内报红**。
> **「（拟）」不是装饰，是"检查抑制器"。** 已修，并把这条记进 §9.2 ③ 和事件索引。

**没结的**：
- ⚠️ **§9.2 ③ 的"拆完要同步"清单原版漏了 `体检.ps1`** —— 已补，但**同类风险还在**：检查 1 依赖的是**文件内部的一段内容**，**检查 5/6 都抓不到这种断法**
- ⚠️ **检查 6 的已知盲区**：它只验"节号存不存在"，验不了"**指对了没有**"。建议 §8 加一行认账 —— **未拍板**
- ⚠️ **本文件第 67 行**（旧条目里）也把收尾清单写成 `学习规定-AI.md` §2 —— **按"只追加"规矩没改**，留此备注
- ⏸ **第六课重写未开始**（结论见下）

**下一个会话接手要点**：
- **第六课要重写**。审完了：**讲课（§3）大体合规，出题（§2）严重违规**。
  逐字核了 A1、H1 两道，**都是"抄例题"**（H1 连数据都不用改，它的【自检】就是正文示例输出的拼接）；
  按结构看其余 10 道常规题同一形状。**§2.2 的判据一击命中了我连续两课（第五课 A 组 + 第六课 12 题）的同一个错**
- 重写照 `学习规定-AI.md` §6.1 五步走；写答案按 `模板.md` §三（**全部实跑**）
- **先做设计再写题**（学生已把出题/讲题的决定权交给教师 AI）

### 2026-09-22 · 会话：文档体系重构 + 学习规定重写（承接 09-21 第四次讨论）

**这轮读了**：`旧文档\学习规则.md`（全 1112 行）· `旧文档\语法速查.md` · 全部 6 份批改 · `第六课-正文.md`
**体检**：**未跑**（脚本本轮才建，第一次实跑见下）
**做了什么**：
- 建 `旧文档\`，旧《学习规则》《语法速查》移入（**从此不以它们为准**）
- 把 1 份 1112 行的文件拆成 **4 份活文档**：`约定` / `进度` / `语法速查` / 本文件
- 新增 `模板.md`（正文·答案·批改·清单的格式）· `接手.md`（零上下文入口）· `愿景.md`（学生私人备忘）
- **知识点编号化**：79 个 ID。`进度.md` §2 定义、`语法速查` §三 用【归属】引用，**互为校验**
- **规则四加"批改归因"补充条**（归因前先问）；新增**每课收尾清单**（11 项，`约定.md` 附 D）
- 建 `体检.ps1`：4 项机械检查，红/绿 + 退出码
- 把结论写进 `约定.md` 附 E（C# 经验怎么用，原旧文档 §六 差点漏搬）

**下半场（同一会话，架构被另一个会话改过之后）**：
- **7 份文档顶部统一加指针块**（`愿景.md` 按学生要求**排除** ——"这东西没有阅读意义"）
- **重写 `学习规定-AI.md`**：补上旧版**整块缺的教学法** —— 出题 §2 / 讲课 §3 / 批改归因 §4 / 复习真题 §5，
  加 **§8 明写"这些地方没有判据"**，末尾附**事件索引**（哪条规则是哪次事故换来的）
- **`体检.ps1` 扩到 7 项**：新增**交叉引用有效性** + **节引用有效性** + **文档体量预警**（阈值与处置见 `学习规定-AI.md` §9）
- **15 处指向作废文件的引用**改指到活文档；补 `模板.md` §六（会话流水模板 —— 我原先引用了它，但它当时不存在）
- **命名例外定案**：3 处收为例外，并把「**已交付给学生的文件不追改名**」写成原则，落进 `模板.md` §一

**改了什么文件**：根目录 7 份 + `体检.ps1`；`旧文档\` 新增 2 份

> ### ⚠️ 本条之后，架构又变了一次（2026-09-22 稍晚，**另一个会话**做的）
>
> 写这条的会话**没有参与**，以下是后来从磁盘核对出来的：
>
> | 变化 | 结果 |
> |---|---|
> | `约定.md` 按**约束对象**拆成两份（**学生拍板**） | `约定-学生.md`（活）· `约定-教师.md`（**已作废**，在 `旧文档\`） |
> | **`学习规定-AI.md` 接替 `约定-教师.md`** | 它是**新文件**（原则式重写），不是搬运 |
> | ⚠️ **每课收尾清单曾随 `约定-教师.md` 一起变成无归属** | 本会话把它救回 **`学习规定-AI.md` §2** |
> | 伊伯战区原文 `旧文档\AI 工作方法.md` 被拷进项目 | 学习项目的适配版是 `学习规定-AI.md`（已在原文顶部标注） |
> | `愿景.md` 误被 `git add -A` 推上 GitHub | 已写进 `.gitignore` |
> | **`流水.md` 被我删掉、换成 `备忘.md`** | **造成 `接手.md` 3 处死引用——已修** |

**验证**：
```
命令： & "D:\studydemo\python学习\体检.ps1"
输出： 结果：绿 —— 通过 27 · 失败 0 · 警告 9
说明： 9 条警告全是"已接受的命名例外"或"历史叙述里的作废文件名"，无一待修
```
> **中途红过两次，两次都是本会话自己引入的**：
> ① `复习一\code\bing2.md` 少了路径前缀 → 悬空引用；
> ② 我把 `§6.2` 写进了 `语法速查` 的【归属】行 → 被 ID 校验当成知识点 `6.2`。
> **②直接促成了新增"节引用有效性"这项检查** —— 检查项是被自己的错逼出来的。

**没结的**：
- ① `约定-教师.md` 作废时，**能立成规则的已迁入** `学习规定-AI.md`；**不可判定的那些明写在 §8**
      —— 不是丢掉，是**标明"这块没有判据、靠现场判断"**
- ② `旧文档\AI 工作方法.md`（伊伯战区原文）—— **2026-09-22 已从根目录移入 `旧文档\`**
- ③ `体检.ps1` **必须存成 UTF-8 with BOM** —— Windows PowerShell 5.1 读无 BOM 的 UTF-8 会按 ANSI 解、中文全乱、脚本直接报 `Unexpected token`
- ④ `进度.md` §6 还有 **5 条待议**（规则三张力 / 阶梯排序 / I3 落空补不补 / 第四类失分 / 终点线没计划）

**下一个会话接手要点**：
- **开课前先跑 `体检.ps1`**，把输出贴进本文件新的一条
- **第六课题目已出 16 道，`第六课\code\` 是空的** —— 学生还没开始做
- 批改完**不是收尾**，回去走 `学习规定-AI.md` §2 那 11 项

---

