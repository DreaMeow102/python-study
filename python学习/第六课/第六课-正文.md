# 第六课：字符串方法

> **本课目标（能力阶梯第 4 级）**
>
> > **学完你能：切分和处理文本。**
>
> **题量**：16 题（A 1 + B 2 + C 2 + D 3 + E 1 + F 1 + G 2 + H 1 + 特殊题 3）
> **预计时间**：4~5 小时（含做题）
> **覆盖**：二级大纲 **(二) 基本数据类型·字符串** + **(六) 字符串格式化**
>
> **这是第 1 段（第 1~5 级）的最后一格。做完这一课，第 1 段收官。**

---

## 怎么用这一课

结构照旧：**理论（短）→ 例题 → ★ 对应习题**。

**每题标了类别**：【新】必须跑通 /【复习】跑一遍确认。

**本课有三道特殊题**（和前两课一样，但它们现在分担三种不同训练）：

| 题 | 是什么 | 规则 |
|---|---|---|
| **I1** | **读官方文档自学**（我不讲） | 规则七 |
| **I2** | **改错题**（给能跑但结果不对的代码） | 规则八 |
| **I3** | **读代码**（说出它在做什么、为什么） | `约定.md` §四·读代码 |

**代码写在 `第六课\code\`，文件名用 `a1.py` / `b1.py` / … / `i3.py`。**

---

## 0. 前面几课留下的最大别扭

**字符串你早就在用了。但到现在为止，你只能"整体看"它。**

```python
s = "apple,banana,cherry"
# 你能：len(s)、s[0]、s[1:5]、s[::-1]、for c in s、"," in s
# 你不能：把它切成一瓣一瓣、把几瓣拼回去、把脏东西洗掉
```

**这就是为什么二级操作题你一道都做不了。**

真题里的"中文分词写文件""价值链 · 词频 Top10"——**核心全是"把一段文本切开、数一数、再拼起来"**。
第五课给了循环，**这一课给循环真正要加工的材料**。

### ★ 先立一张"词面陷阱表"（和速查里那张同一套逻辑）

**中文说法**最容易把人带偏的是这几个：

| 中文说法 | 字面容易想到 | **代码里应该用** |
|---|---|---|
| **切分 / 拆开 / 分成几瓣** | 切片 `[a:b]` | **`split()`** |
| **拼起来 / 接起来 / 连成一串** | `+` 一个个加 | **`join()`** |
| **去掉空格** | `replace(" ", "")` | **`strip()`** ← 而且它只管**首尾** |
| 找某个词在不在 | 只能 `in` | `find()`（要知道**位置**）、`count()`（要知道**几次**） |
| 这段文字**是不是**数字 | `int(s)` 试一下 | `s.isdigit()` |

> **切片和 `split` 是两件完全不同的事**，这是最容易混的一对：
>
> | | 按什么切 | 结果 |
> |---|---|---|
> | **切片** | **位置** | 还是**一个**字符串 |
> | **`split`** | **内容（分隔符）** | **一个列表** |

---

# 第 1 节 · 第一件事：字符串是**不可变**的

**这一节是本课的地基。地基歪了，后面每一节都会跟着歪。**

## 1.1 现场看报错

```python
name = "python"
name[0] = "P"
```

**实际输出：**

```
TypeError: 'str' object does not support item assignment
```

**读这条报错**：`str` 对象**不支持给某一项赋值**。

**而列表是可以的**（第三课学过）：

```python
nums = [1, 2, 3]
nums[0] = 99        # ✅ 行
name[0] = "P"       # ❌ 不行
```

| | 能不能改其中一个元素 |
|---|---|
| **列表 `list`** | ✅ **能**（可变的） |
| **字符串 `str`** | ❌ **不能**（**不可变的**） |

> **顺便解释第三课那个伏笔**：`b = a` 让两个名字指向**同一个列表**，所以改一个另一个也变。
> **字符串根本没有这个问题**——因为它**改不了**，压根不会有"另一个也变"这种事。
> （`b = s` 之后你怎么折腾 `b`，`s` 都不会变。）

## 1.2 所以：**所有字符串方法都是"返回一个新串"**

**这一条是本课最重要的规矩。**

```python
name = "python"

big = name.upper()      # ✅ 接住新串
print(big)              # PYTHON

name.upper()            # ❌ 忘了接住 —— 等于这行白写
print(name)             # python   （一点没变！）
```

**实际输出：**

```
PYTHON
python
```

> **这就是易错点第 1 条（返回值必须接住）的字符串版本。**
> 你在 `float()` 上跨过这道坎了，**现在换了个马甲又出现一次**。
>
> **判断法**：**一行只有 `s.方法()` 而左边没有 `=`，99% 是错的。**
> （唯一例外是后面 §2/§3 里"只为了看结果"的 `print(s.find(...))`。）

## 1.3 那怎么"改"字符串？

**答案：拼一个新的。**

```python
s = "cat"
s = "b" + s[1:]     # ✅ 造一个新串，再赋回去
print(s)            # bat
```

**读是可以的**（读不会改变它）：`name[0]` → `p`。

---

## ★ A 组习题 · 字符串不可变（1 题）

### A1 · 三种写法，三种结果 【新】

**要求**：新建 `a1.py`。

```python
name = "python"
```

1. **试着**把第一个字符改成大写 `P` —— **它会报错**。
   把这行**注释保留**在文件里，并在旁边写上**报错原文**（规则十二：留痕 + 文件能跑）。
2. 用 `upper()` 拿到全大写，**打印出来**。
3. **单独写一行** `name.upper()`（不接住），然后再打印 `name`——看看变没变。
4. 用**索引**读出第一个字符并打印（读是允许的）。

**【自检】**（真实输出）

```
PYTHON
python
p
```

> **三行输出对应第 2、3、4 步。第 3 步那两行是重点**：
> **`name.upper()` 那一行什么都没干，`name` 还是 `python`。**

---

# 第 2 节 · 找：`find` / `index` / `count` / `startswith` / `endswith`

**这一节全是"问字符串一个问题"。它们都**不改变**原串。**

## 2.1 `find` —— 找不到给 **-1**

```python
s = "hello world"
print(s.find("o"))      # 4   第一次出现在下标 4
print(s.rfind("o"))     # 7   最后一次出现在下标 7（r = right，从右往左找）
print(s.find("z"))      # -1  找不到
```

**实际输出：**

```
4
7
-1
```

**注意**：`find` **永远不报错**，找不到就给 `-1`。

## 2.2 `index` —— 找不到就**报错**

```python
s.index("z")
```

**实际输出：**

```
ValueError: substring not found
```

| 方法 | 找不到时 |
|---|---|
| **`find`** | 返回 **`-1`** |
| **`index`** | **报错** `ValueError: substring not found` |

> **选择题常考这个差别。** 实际写代码时**优先用 `find`**——因为它不炸。
> 想判断"在不在"，更直接的是第三课学过的 **`"z" in s`**（给 `True` / `False`）。

## 2.3 `count` —— 数出现几次

```python
text = "banana"
print(text.count("an"))     # 2
print(text.count("a"))      # 3
```

**实际输出：**

```
2
3
```

> **冷知识（选择题爱考）**：`"abc".count("")` 是 **4**，不是 0。
> 空字符串被认为"在每个位置之间都出现一次"，3 个字符的串有 4 个缝隙。

## 2.4 `startswith` / `endswith` —— 开头 / 结尾是不是它

```python
filename = "2026-09-21.txt"
print(filename.startswith("2026"))     # True
print(filename.endswith(".txt"))       # True
```

**实际输出：**

```
True
True
```

> ⚠️ **是 `startswith` / `endswith`，不是 `startswiths`。**中间**没有** `s`。
> （`start` + `s` + `with` → **`startswith`**，别被"是不是"带出一个多余的 s。）

---

## ★ B 组习题 · 找（2 题）

### B1 · 找位置 【新】

**要求**：新建 `b1.py`。

```python
s = "hello world"
```

1. 打印 `"o"` **第一次**出现的位置
2. 打印 `"o"` **最后一次**出现的位置
3. 打印 `"z"` 在不在——用 **`find`** 的方式
4. **试一次** `s.index("z")`，把报错原文**注释保留**
5. 再用第三课的 **`in`** 判断 `"z"` 在不在
6. 打印 `"world"` 出现的位置

**【自检】**（真实输出）

```
4
7
-1
False
6
```

> **第 3、5 步是同一个问题的两种问法**：`find` 给 **`-1`**，`in` 给 **`False`**。都要会。

---

### B2 · 文件名检查器 【新】

**要求**：新建 `b2.py`。

```python
filename = "2026-09-21.txt"
text = "banana"
```

1. 判断 `filename` 是不是以 `"2026"` 开头
2. 判断 `filename` 是不是以 `".txt"` 结尾
3. 数一数 `filename` 里有几个 `"-"`
4. **用 `if` 判断**：如果以 `.txt` **或** `.md` 结尾，打印 `是文本文件`，否则打印 `不是文本文件`
5. 数一数 `text` 里 `"an"` 和 `"a"` 各出现几次

**【自检】**（真实输出）

```
True
True
2
是文本文件
2
3
```

> **第 4 步同时复习了第四课的 `or`**——两个条件只要一个成立就算。

---

# 第 3 节 · 改：`replace` / `upper` / `lower` / `strip`

**再强调一遍：它们**都返回新串**，**都不改原串**。**

## 3.1 `replace(old, new)` —— 替换

```python
text = "I like cat. cat is cute."

new = text.replace("cat", "dog")        # ✅ 接住
print(new)
print(text)                             # 原串没变

print(text.replace("cat", "dog", 1))    # 只换前 1 个
print(text.replace("c", "C"))           # 所有 c 都换
```

**实际输出：**

```
I like dog. dog is cute.
I like cat. cat is cute.
I like dog. cat is cute.
I like Cat. Cat is Cute.
```

**第三个参数是"最多换几次"**：`replace("cat", "dog", 1)` 只换**前 1 个**。

> **第 2 行是重点**：`text` 一点没变。**`replace` 不会改 `text`，它给你一个新串。**

## 3.2 `upper` / `lower`

```python
print("abc".upper())     # ABC
print("ABC".lower())     # abc
```

**顺带认识两个（二级"了解"级，见到认得出就行）：**

| 方法 | 效果 | 例子 |
|---|---|---|
| `.title()` | 每个单词首字母大写 | `"abc def".title()` → `Abc Def` |
| `.capitalize()` | **只有整句第一个**字母大写 | `"abc def".capitalize()` → `Abc def` |

## 3.3 `strip` —— 它到底在去什么？

**`strip()` 不写参数 = 去掉首尾的"空白"（空格、Tab、换行）。**

```python
print("  x  ".strip())      # x
```

**它去的是"首尾"，中间不管：**

```python
print("  a  b  ".strip())   # 'a  b'  ← 中间那两个空格还在
```

> ### ⚠️ 陷阱：`strip("x")` 不是"去掉开头的 x"
>
> ```python
> print("xxaxx".strip("x"))     # 'a'
> ```
>
> **`strip` 的参数是"要剥掉的字符集合"**，不是前缀。它会把**首尾所有属于这个集合的字符**都剥掉。
>
> **所以 `strip("x")` 会把两头的 x 全剥光，剥到不是 x 为止。**
> 想"去掉开头两个字符"要用**切片** `s[2:]`，不是 `strip`。

**为什么这个方法这么重要**：`input()` 读进来的字符串**带着行尾的换行符**。

```python
s = input()          # 你敲了 yes 回车，s 其实是 "yes\n"
if s == "yes":       # ❌ 不成立！因为多了一个 \n
if s.strip() == "yes":   # ✅
```

## 3.4 链式调用

**方法返回的还是字符串，所以可以接着点下去：**

```python
"   YeS  ".strip().lower()     # 'yes'
```

**读法：先 `strip()` 得到一个干净的串，再对它 `.lower()`。**

**`.lower().strip()` 反着写结果也一样**（这两个互不干扰）——但**顺序在别的组合里会有影响**，见下面 D1。

---

## ★ C 组习题 · 改（2 题）

### C1 · 清洗一行输入 【新】

**要求**：新建 `c1.py`。

```python
raw = "   YeS  "
messy = "  yes \n"
```

1. 打印 `raw` 去掉首尾空白的结果
2. 打印 `raw` 先 `strip` 再 `lower` 的结果（**链式**）
3. 打印 `raw` 先 `lower` 再 `strip` 的结果，确认和上一步一样
4. 把 `raw` 清洗成小写并**接住**，**用 `if` 判断**：等于 `"yes"` 就打印 `继续`，否则打印 `停止`
5. 用 **`f"[{...}]"`** 打印 `messy` **和** `messy.strip()`——**用方括号看清空白到底在哪**

**【自检】**（真实输出；第 5 步的换行是真的换行）

```
YeS
yes
yes
继续
[  yes 
]
[yes]
```

> **第 5 步那对方括号是故意的**：直接 `print` 看不出空格和换行，
> **套上方括号，脏东西就现形了**。以后怀疑"多了看不见的字符"，都用这一招。

---

### C2 · 批量替换 【新】

**要求**：新建 `c2.py`。

```python
text = "I like cat. cat is cute."
```

1. 把所有 `"cat"` 换成 `"dog"`，**接住**并打印
2. **紧接着打印 `text`**，确认原串没变
3. 用第三个参数**只换前 1 个** `"cat"`，打印
4. 把所有 `"c"` 换成 `"C"`，打印

**【自检】**（真实输出）

```
I like dog. dog is cute.
I like cat. cat is cute.
I like dog. cat is cute.
I like Cat. Cat is Cute.
```

---

# 第 4 节 · 切与拼：`split` / `join`（本课核心）

> **这一节是二级操作题的地基。** 文本处理的每一步都从这里开始。

## 4.1 `split()` 不写参数 = 按**任意空白**切

**这是最容易想错的一个细节。**

```python
messy = "  a  b  "
print(messy.split())        # ['a', 'b']
print(messy.split(" "))     # ['', '', 'a', '', 'b', '', '']   ← 完全不一样！
```

**实际输出：**

```
['a', 'b']
['', '', 'a', '', 'b', '', '']
```

| 写法 | 怎么切 | 结果 |
|---|---|---|
| **`split()`** | 按**任意长度的空白**切，**并且丢掉空段** | `['a', 'b']` —— **2 个** |
| `split(" ")` | **严格按"一个空格"**切，**空段全保留** | `['', '', 'a', '', 'b', '', '']` —— **7 个** |

> **一句话**：**不写参数 = 智能切；写了参数 = 死板切。**
> **处理"人打的字"用 `split()`；处理"格式固定的数据"才写参数。**

## 4.2 `split(",")` 指定分隔符

```python
line = "apple,banana,,cherry"
parts = line.split(",")
print(parts)            # ['apple', 'banana', '', 'cherry']
print(len(parts))       # 4
print(f"[{parts[2]}]")  # []   ← 第三个是空字符串
```

**实际输出：**

```
['apple', 'banana', '', 'cherry']
4
[]
```

> **⚠️ 关键**：**两个逗号之间什么都没有，就会切出一个空字符串**。
> `split` **不会**帮你把空字段扔掉。这正是 I2 改错题要处理的东西。

## 4.3 `split(分隔符, 最多切几次)`

```python
path = "D:/study/python/lesson6.txt"
print(path.split("/", 1))     # ['D:', 'study/python/lesson6.txt']
```

**实际输出：**

```
['D:', 'study/python/lesson6.txt']
```

**只切 1 次** → 得到 **2 个**元素：`['D:', 'study/python/lesson6.txt']`。

> **记法**：`split(sep, n)` 的结果**最多有 `n + 1` 段**。
> 用途：**只想从左边剥掉一层**的时候（比如拆 `键=值` 只拆第一个 `=`）。

## 4.4 `join` —— 方向正好**相反**

**`split` 把字符串变列表；`join` 把列表变字符串。**

```python
print("-".join(["2026", "09", "21"]))     # 2026-09-21
print("".join(["p", "y", "t", "h", "o", "n"]))   # python
```

**实际输出：**

```
2026-09-21
python
```

**读法**：**`"分隔符".join(列表)`** —— 用这个分隔符，把列表里的东西**串起来**。

> ### ⚠️ 注意方向！这是全课最容易写反的一处
>
> | 想要 | 写 | 谁在前 |
> |---|---|---|
> | 字符串 → 列表 | **`s.split(",")`** | **分隔符写在括号里** |
> | 列表 → 字符串 | **`",".join(列表)`** | **分隔符写在点前面** |
>
> **`join` 是唯一一个"分隔符在前、列表在后"的写法**，因为它本质是**分隔符的方法**。

> ### ⚠️ `join` 只能拼**字符串**
>
> ```python
> "".join([1, 2, 3])
> ```
>
> **实际输出：**
>
> ```
> TypeError: sequence item 0: expected str instance, int found
> ```
>
> **读报错**：`item 0`（第 0 项）期待的是 `str`，实际给了 `int`。
> **要拼数字，先用 `str()` 转换。**

## 4.5 数字列表 → 字符串：两种做法

```python
nums = [1, 2, 3]

# 做法一：循环 + 累加（第五课的死格式）
s = ""
for n in nums:
    s = s + str(n)          # 累加器 + str() 转换
print(s)                    # 123

# 做法二：join + 先把每个元素转成字符串
# 注：这种做法要配合推导式或循环，第七课之后再回来看
```

**实际输出：**

```
123
```

> **注意这里是第五课的累加器原封不动搬到字符串上**：
> `total = total + n` 变成了 `s = s + str(n)`。**同一个模式，换了材料。**

---

## ★ D 组习题 · `split` / `join`（3 题）

### D1 · 切开一行数据 【新】

**要求**：新建 `d1.py`。

```python
line = "apple,banana,,cherry"
messy = "  a  b  "
```

1. 把 `line` 按 `","` 切开，打印**结果列表**和**长度**
2. 用 **`f"[{...}]"`** 打印 `parts[2]`，看清它到底是什么
3. 打印 `messy.split()` 的结果**和长度**
4. 打印 `messy.split(" ")` 的结果**和长度**
5. **写一句注释**：这两种写法的区别是什么？

**【自检】**（真实输出）

```
['apple', 'banana', '', 'cherry']
4
[]
['a', 'b']
2
['', '', 'a', '', 'b', '', '']
7
```

> **第 3、4 步是本课最值钱的一组对比**：同一个字符串，**2 个** vs **7 个**。
> **差别只在"括号里写没写东西"。**

---

### D2 · 拆路径 【新】

**要求**：新建 `d2.py`。

```python
path = "D:/study/python/lesson6.txt"
```

1. 按 `"/"` 切开，打印列表
2. 用**负索引**取出最后一段（文件名）并打印（第三课学的）
3. 打印第一段（盘符）
4. 用 `"/".join(...)` 把列表**拼回去**，打印
5. 打印 `path == "/".join(parts)`，验证拼回去的和原来一样
6. 打印 `path.split("/", 1)`，看看只切一次是什么效果

**【自检】**（真实输出）

```
['D:', 'study', 'python', 'lesson6.txt']
lesson6.txt
D:
D:/study/python/lesson6.txt
True
['D:', 'study/python/lesson6.txt']
```

> **第 5 步的 `True` 很重要**：`split` 和 `join` 是**互逆**的一对。
> **切开的能原样拼回去，说明它是"无损的"。**

---

### D3 · 拼装数字 【新】

**要求**：新建 `d3.py`。

1. 用 `join` 把 `["2026", "09", "21"]` 拼成 `2026-09-21`（分隔符用 `"-"`）
2. 用 `join` 把 `["p","y","t","h","o","n"]` 拼成 `python`（分隔符用 `""`）
3. **试着** `"".join([1, 2, 3])`——它会报错。把**报错原文注释保留**，并在旁边写一句：为什么报错
4. 用**循环 + `str()`** 把 `[1, 2, 3]` 拼成 `"123"`，打印
5. 打印 `str(nums[0]) + "-" + str(nums[1])`
6. 打印 `s == "123"`

**【自检】**（真实输出）

```
2026-09-21
python
123
1-2
True
```

> **第 1、3 步放一起看**：`join` **只能吃字符串**。
> 第 1 步能过，是因为列表里**本来就是字符串**；第 3 步炸，是因为**是数字**。

---

# 第 5 节 · 判断：`isdigit` / `isalpha` / `isspace` / `isalnum`

**这一组方法全部返回 `True` / `False`。**二级选择题用它们出题非常频繁。

## 5.1 四个方法

| 方法 | 问的是 |
|---|---|
| `.isdigit()` | 是不是**全是数字字符** |
| `.isalpha()` | 是不是**全是字母** |
| `.isspace()` | 是不是**全是空白** |
| `.isalnum()` | 是不是**全是数字或字母**（alnum = alphanumeric） |

## 5.2 实际跑一遍（**预测之后再对照**）

```python
tests = ["123", "-1", "3.14", "abc", "a1", " ", "", "１２３"]
for t in tests:
    print(f"[{t}] isdigit={t.isdigit()} isalpha={t.isalpha()} isspace={t.isspace()}")
```

**实际输出：**

```
[123] isdigit=True isalpha=False isspace=False
[-1] isdigit=False isalpha=False isspace=False
[3.14] isdigit=False isalpha=False isspace=False
[abc] isdigit=False isalpha=True isspace=False
[a1] isdigit=False isalpha=False isspace=False
[ ] isdigit=False isalpha=False isspace=True
[] isdigit=False isalpha=False isspace=False
[１２３] isdigit=True isalpha=False isspace=False
```

> ### ⚠️ 三个反直觉的地方（选择题就考这些）
>
> | 现象 | 为什么 |
> |---|---|
> | **`"-1".isdigit()` → `False`** | **负号不是数字字符**。它只认 `0-9` |
> | **`"3.14".isdigit()` → `False`** | **小数点也不是数字字符**。`isdigit` ≠ "能不能转成数" |
> | **`"".isspace()` → `False`** | **空串什么都不满足**（连"全是空白"都不算） |
>
> **另外两条值得记：**
>
> | 现象 | 说明 |
> |---|---|
> | **`"１２３".isdigit()` → `True`** | 全角数字**也是数字字符**（`isdigit` 认它） |
> | **`"你好".isalpha()` → `True`** | **中文汉字也算字母**（Python 认的是 Unicode 分类，不是"英文"） |
>
> **所以 `isdigit()` 的准确含义是"全是数字类字符"，不是"这是个合法的整数"。**
> **要判断"能不能转成整数"，最稳的还是 `try`（第十课会学）。**

## 5.3 用法：转数字之前先问一句

```python
s = "2026"
if s.isdigit():
    print(int(s) + 1)       # 2027
else:
    print("不是纯数字")
```

**实际输出：**

```
2027
```

---

## ★ E 组习题 · 判断类方法（1 题）

### E1 · 分类器 【新】

**要求**：新建 `e1.py`。

1. 建一个列表，装这 8 个值：`"123"`、`"-1"`、`"3.14"`、`"abc"`、`"a1"`、`" "`、`""`、`"１２３"`
2. 用 `for` 遍历它（第五课），每个都用 **`f"[{t}] isdigit=... isalpha=... isspace=..."`** 打印三个判断结果
3. **先猜再跑**：跑之前先写下你认为 `"-1"` 和 `"3.14"` 的结果，跑完对照
4. 用 `if s.isdigit()` 判断 `"2026"`，成立就打印 `int("2026") + 1`
5. 打印 `"a1".isalnum()` 和 `"你好".isalpha()`

**【自检】**（真实输出）

```
[123] isdigit=True isalpha=False isspace=False
[-1] isdigit=False isalpha=False isspace=False
[3.14] isdigit=False isalpha=False isspace=False
[abc] isdigit=False isalpha=True isspace=False
[a1] isdigit=False isalpha=False isspace=False
[ ] isdigit=False isalpha=False isspace=True
[] isdigit=False isalpha=False isspace=False
[１２３] isdigit=True isalpha=False isspace=False
2027
True
True
```

> **第 3 步是这道题的重点**——**先写下预测，再跑**。
> 预测错了才有价值：那说明脑子里的模型和 Python 的实际行为不一致。
>
> **我们刚在第五课 F1 上吃过这个教训**（你想的 `range` 和实际的不一样）。
> **"先猜 → 再跑 → 有出入就核对"，这个顺序比"直接跑"值钱得多。**

---

# 第 6 节 · 格式化：`format()`

**你已经会 f-string 了。`format()` 是它的"老前辈"，但二级会考，所以要认识。**

## 6.1 三种写法

```python
name = "小明"
score = 95.5

print("{} 的分数是 {}".format(name, score))       # 按顺序填空
print("{0} 的分数是 {1}".format(name, score))     # 按下标填
print("{1} 分，{0} 同学".format(name, score))     # 下标可以调换顺序
print("{n} 的分数是 {s}".format(n=name, s=score)) # 按名字填
```

**实际输出：**

```
小明 的分数是 95.5
小明 的分数是 95.5
95.5 分，小明 同学
小明 的分数是 95.5
```

| 写法 | 特点 |
|---|---|
| `{}` | **按位置顺序**填，最简单 |
| `{0}` `{1}` | **按编号**填，**可以重复用同一个**（`{0}{1}{0}`）、**可以换顺序** |
| `{name}` | **按关键字**填，`format(n=..., s=...)`。**顺序随便** |

## 6.2 和 f-string 的关系

```python
print(f"{name} 的分数是 {score}")     # 和上面第一条完全一样
```

**实际输出：**

```
小明 的分数是 95.5
```

> **f-string 是 `format()` 的语法糖**（更好写的版本）。
> **实际写代码用 f-string；但二级选择题会让你读 `format()`。**两种都要认得出。

## 6.3 ⚠️ 格式说明符**这一节不讲**

**`{:>10}`、`{:.2f}`、`{:,}` 这一套（冒号后面那部分）——**

| | |
|---|---|
| **归谁讲** | **你自己读官方文档**（规则七） |
| **在哪** | **I1 题** |

**所以这一节到这里为止。**

---

## ★ F 组习题 · `format()` 基本用法（1 题）

### F1 · 三种填空方式 【新】

**要求**：新建 `f1.py`。

```python
name = "小明"
score = 95.5
```

1. 用 `{}` **按位置**输出 `小明 的分数是 95.5`
2. 用 `{0}` `{1}` 输出**同样的句子**
3. 用 `{1}` `{0}` **调换顺序**输出 `95.5 分，小明 同学`
4. 用**关键字** `{n}` `{s}` 输出 `小明 的分数是 95.5`
5. 用 **f-string** 输出一遍第 1 步的句子，确认结果一样

**【自检】**（真实输出）

```
小明 的分数是 95.5
小明 的分数是 95.5
95.5 分，小明 同学
小明 的分数是 95.5
小明 的分数是 95.5
```

> **注意第 4 行**：`score` 是 `95.5`，**用 `{}` 直接打印不会变成 `95.50`**。
> **"保留两位小数"是格式说明符干的活**——那部分在 I1。

---

# 第 7 节 · 字符与编码：`ord` / `chr` / `bin` / `oct` / `hex`

> **这一节是"纯选择题细节"。** 操作题几乎不用，但选择题拿高分必须有它。

## 7.1 `ord`：字符 → 编码数字；`chr`：数字 → 字符

```python
print(ord("a"))      # 97
print(ord("A"))      # 65
print(ord("0"))      # 48
print(chr(97))       # a
```

**实际输出：**

```
97
65
48
a
```

### ★ 这三个数字要背下来

| 字符 | 编码 |
|---|---|
| `"0"` | **48** |
| `"A"` | **65** |
| `"a"` | **97** |

**顺带记住两条推论（选择题常考）：**

```python
print(ord("a") - ord("A"))      # 32
```

**实际输出：**

```
32
```

1. **大写和小写相差 32**（小写更大）
2. **`"a"` 到 `"z"` 是连续的**，所以 `chr(ord("k") - 32)` 就是它的大写

```python
c = "k"
print(chr(ord(c) - 32))     # K   ← 手算出来
print(c.upper())            # K   ← 本课方法
```

**实际输出：**

```
K
K
```

> **同一件事两种做法**：`ord`/`chr` 是"底层做法"，`upper()` 是"高层做法"。
> **知道它们等价，你就同时看懂了编译原理的味道和 Python 的设计意图。**

## 7.2 `bin` / `oct` / `hex`：转成二 / 八 / 十六进制**字符串**

```python
print(bin(10))       # 0b1010
print(oct(10))       # 0o12
print(hex(255))      # 0xff
print(hex(10))       # 0xa
```

**实际输出：**

```
0b1010
0o12
0xff
0xa
```

> ### ⚠️ 三个坑（选择题全在这儿）
>
> | 坑 | 说明 |
> |---|---|
> | **返回值是字符串** | `type(bin(10))` → `<class 'str'>` |
> | **带前缀** | 二进制 `0b`、八进制 `0o`、十六进制 `0x`。**前缀算在长度里** |
> | **长度不是你想的那个数** | `len(bin(10))` 是 **6**（`0b1010`），**不是 4** |
>
> ```python
> print(len(bin(10)))          # 6
> print(type(bin(10)))         # <class 'str'>
> ```
>
> **实际输出：**
>
> ```
> 6
> <class 'str'>
> ```

---

## ★ G 组习题 · 字符与编码（2 题）

### G1 · `ord` / `chr` 手算大小写 【新】

**要求**：新建 `g1.py`。

1. 打印 `ord("a")`、`ord("A")`、`ord("0")`
2. 打印 `chr(97)`
3. 打印 `ord("a") - ord("A")`，并**注释写下这个差的含义**
4. 令 `c = "k"`，用 **`chr(ord(c) - 32)`** 手算出它的大写并打印
5. 用 **`c.upper()`** 打印同一个结果，确认两者一致

**【自检】**（真实输出）

```
97
65
48
a
32
K
K
```

---

### G2 · 进制转换的返回值 【新】

**要求**：新建 `g2.py`。

1. 打印 `bin(10)`、`oct(10)`、`hex(255)`、`hex(10)`
2. 打印 `len(bin(10))`——**先猜再跑**
3. 打印 `type(bin(10))`
4. **注释写下**：为什么第 2 步的结果不是你以为的那个数

**【自检】**（真实输出）

```
0b1010
0o12
0xff
0xa
6
<class 'str'>
```

> **第 2 步如果你猜的是 4，就把原因写清楚**：`0b` 前缀**也是字符**，占了 2 个长度。
> 顺便想一下：`len(hex(255))` 应该是几？（**4**，因为 `0xff` 是 4 个字符。）

---

# 第 8 节 · 字面量里的细节

**这一节全是"写在引号里面的东西"。它们都是选择题的常客。**

## 8.1 转义字符：`\n` / `\t` / `\\`

**反斜杠 `\` 加上一个字母，表示一个"看不见的字符"。**

| 写法 | 是什么 | 占几个字符 |
|---|---|---|
| `\n` | **换行** | **1** |
| `\t` | **Tab（制表符）** | **1** |
| `\\` | 一个反斜杠本身 | 1 |
| `\"` | 一个引号本身（引号里要打引号时用） | 1 |

```python
print("第一行\n第二行")
print(len("a\nb"))        # 3 —— \n 只占 1 个位置
```

**实际输出：**

```
第一行
第二行
3
```

> **`"a\nb"` 看起来是 4 个字符，实际是 3 个**：`a`、换行、`b`。**这是选择题陷阱。**

**`\t` 的实际用途是"对齐"：**

```python
print("姓名\t年龄")
print("小明\t18")
```

**实际输出：**

```
姓名	年龄
小明	18
```

## 8.2 原始字符串 `r"..."`：让 `\` 不再特殊

```python
print(r"a\nb")        # a\nb   ← 四个字符，\ 和 n 分开的
```

**实际输出：**

```
a\nb
```

| 写法 | `len` | 内容 |
|---|---|---|
| `"a\nb"` | **3** | a、换行、b |
| **`r"a\nb"`** | **4** | a、`\`、n、b |

> **`r` 的用途**：**路径**和**正则表达式**。Windows 路径 `r"C:\new\test"` —— 不加 `r`，
> `\n` 会被当成换行、`\t` 会被当成 Tab，路径就毁了。**（第九课读文件时你会天天用。）**

## 8.3 三引号：一次写多行

```python
print("""
第一行
第二行
""")
```

**实际输出：**（**注意首尾各有一个空行**——这是真的）

```

第一行
第二行

```

> **三引号 `"""..."""` 里的换行是"真的换行"**，不用写 `\n`。
> **注意**：紧跟在 `"""` 后面的那个换行**也会被算进去**，所以输出**开头有个空行**。

## 8.4 字符串乘法 `*`

```python
print("-" * 30)
```

**实际输出：**

```
------------------------------
```

**字符串 `*` 整数 = 重复那么多次。**用途：**打印分隔线**、造测试数据。

> ### ⚠️ 顺便补一个你上次真题里丢分的点
>
> **真题实战一的选择题里有一道 `"a" + "b" * 2`，你答成了 `abab`。**
>
> **正确答案是 `"abb"`** —— 因为 **`*` 的优先级高于 `+`**：
>
> ```
> "a" + "b" * 2
>   ↓ 先算乘法
> "a" + "bb"
>   ↓ 再算加法
> "abb"
> ```
>
> **（`("a" + "b") * 2` 才是 `"abab"`。）**
>
> **这题已经考过，不会再考第二遍**（规则三）。
> **但它是你真题里的一个缺口，写在这里，让你把"`*` 比 `+` 优先"这件事记住。**

---

## ★ H 组习题 · 字面量细节（1 题）

### H1 · 把看不见的东西打出来 【新】

**要求**：新建 `h1.py`。

1. 打印一条 **30 个 `-`** 的分隔线（用 `*`，不要手打 30 个）
2. 用 `\t` 打印两行对齐的表（表头 `姓名` `年龄`，数据 `小明` `18`）
3. 用 `\n` 在一个 `print` 里打印两行
4. 打印 `len("a\nb")`——**先猜再跑**
5. 打印 `r"a\nb"`，紧挨着上一行看区别
6. 用**三引号**打印两行文字
7. **注释写下**：第 4 步和第 5 步的结果为什么不同

**【自检】**（真实输出）

```
------------------------------
姓名	年龄
小明	18
第一行
第二行
3
原始串：
a\nb

第一行
第二行

```

> **第 4、5 步是本课最容易混的一对**：`3` vs `4`。
> **不是"哪个对哪个错"，是"`r` 关掉了 `\` 的特殊身份"。**

---

# 第 9 节 · 三道特殊题

## I1 · 读官方文档自学 【新】★ 规则七

> **这个知识点我不讲。** 自己读文档。

### 题目

**你已经会 `{}` 了。但 `{}` 里冒号后面**还能写一整套东西**——**

**你其实早就用过一个：`f"{pi:.2f}"` 里的 `.2f`。那只是这套语法的冰山一角。**

**去读这段官方文档：**

> 👉 [Python 标准库 · 格式规格迷你语言](https://docs.python.org/zh-cn/3/library/string.html#format-specification-mini-language)
>
> （页面很长，**在页面里搜「格式规格迷你语言」**，或搜 `format_spec`）
>
> 配套入口：👉 [格式字符串语法](https://docs.python.org/zh-cn/3/library/string.html#format-string-syntax)

### 要回答的问题（写进 `i1.py` 的注释）

1. **`{:>8}` 里的 `>` 是什么意思？** `>`、`<`、`^` 分别代表什么对齐方式？
2. **`{:*^9}` 里的 `*` 为什么写在最前面？**（提示：文档里那行方括号 `[[fill]align]` 是怎么排序的？）
3. **怎么输出"总宽度 8、左边补 `0`、保留 2 位小数"的数**（拿 `3.14159` 试）？
4. **`{:,}` 和 `{:.1%}` 分别是干什么的？**
5. **格式说明符的完整顺序是什么？**（把那行方括号抄下来）

**【自检】**（把这几条跑出来，真实输出）

```python
print(f"[{'ab':>8}]")
print(f"[{'ab':<8}]")
print(f"[{'ab':^8}]")
print(f"[{'ab':*^9}]")
print(f"[{3.14159:08.2f}]")
print(f"[{1234567:,}]")
print(f"[{0.3456:.1%}]")
print(f"[{-42:+d}]")
```

```
[      ab]
[ab      ]
[   ab   ]
[***ab****]
[00003.14]
[1,234,567]
[34.6%]
[-42]
```

> **用方括号包起来看**——不然你看不出**空格和填充字符**到底在哪。**这是本课第二次用这一招。**

### 交上来时

**只要回答对就行**（判分标准已改，见规则七）：

| 要不要 | |
|---|---|
| 强制写代码 | ❌ **不用** |
| 强制贴文档原文 | ❌ **不用** |
| 判什么 | ✅ **理解对不对**，没答全按理解程度给分 |

> **但那条软提醒还在**：上次（第五课 F1）你的 Q3 想错了。
> **想错了不扣分，但那个错会留在卷子上**——靠刷题兜住（规则十一）。

---

## I2 · 改错题 【新】★ 规则八

> **下面这段代码能跑，不报错——但结果不对。**

### 有问题的代码

```python
line = "  apple , banana ,, cherry  "

line.strip()
parts = line.split(",")
for p in parts:
    print(f"[{p}]")
```

### 现状

**它跑起来输出这个：**

```
[  apple ]
[ banana ]
[]
[ cherry  ]
```

### 应该输出什么

```
[apple]
[banana]
[cherry]
```

**三个字段都干干净净，而且那个空字段不该出现。**

### 要求

1. **先读一遍，在注释里写清楚：一共有几处问题？每处分别是什么？**
2. **改对**，让输出变成上面那样
3. **保留原错误行作注释**（规则十二：留痕 + 文件能跑）
4. 说明：**哪一处问题是本课第 1 节的知识点直接导致的？**

**【自检】**（改对之后）

```
[apple]
[banana]
[cherry]
```

> **这题不给答案。** 参考答案里有正确代码，**先自己做**。
>
> **⚠️ 提示**：三个问题分别属于——
> **① `strip` 的返回值去哪了？ ② `strip` 到底该作用在"整行"还是"每个字段"？ ③ 空字符串该不该打印？**
>
> **注意第 ③ 个**：怎么让一个空字符串"不打印"？**你在第四课学过真值判断**——`if p:` 就够了。

---

## I3 · 读代码 【新】

> **给一段能跑的代码，说出它在做什么、为什么这么写。** 先读、先猜，**不要先跑**。

### 代码

```python
text = "the cat and the dog and the bird"
words = text.split()
longest = words[0]
for w in words:
    if len(w) > len(longest):
        longest = w
print(longest)
```

### 要回答的问题（写进 `i3.py` 的注释）

1. **它在做什么？**（一句话）
2. **输出是什么？先猜，再跑验证。**
3. **`text.split()` 拆出来几个词？**
4. **为什么写 `longest = words[0]` 而不是 `longest = ""`？**
   （提示：想想第五课 B3 找最大时，为什么不能写 `big = 0`。）
5. **⚠️ 这段代码有个隐藏的坑**：如果 `text` 是 `"   "`（**全是空格**），会发生什么？**为什么？**
   （提示：先单独跑一下 `"   ".split()`，看它返回什么。）

**【自检】**（第 2、3 题）

```
bird
8
```

**【自检】**（第 5 题——它会炸，把报错原文写下来）

```
[]
0
IndexError: list index out of range
```

> **第 5 题是这道题的价值所在。**
>
> **一段"看起来完全正确、跑给定数据也没问题"的代码，换一个输入就崩了。**
> **真实开发里 80% 的 bug 都是这种**——不是写错了语法，是**没想到某种输入**。
>
> **第 4 题和第 5 题连起来看，还有个反直觉的结论**：
> 在**这里**写 `longest = ""` 其实是**安全**的（因为 `len("")` 是 0，比任何词都短），
> **但写 `words[0]` 反而不安全**（列表空的时候会崩）。
> **而第五课 B3 里正好相反**——数字的 `0` 不安全、`nums[0]` 才安全。
> **同一个"打擂法"，两种材料的正确答案不一样。想清楚为什么。**

---

# 附录

## A. 本课常见报错速查

> **全部是实跑抓下来的原文**（规则一）。

| 报错 | 原因 | 怎么改 |
|---|---|---|
| `TypeError: 'str' object does not support item assignment` | 想改字符串的某个字符 | **字符串不可变**，只能拼一个新串再赋值 |
| `AttributeError: 'str' object has no attribute 'splits'` | 方法名拼错（`splits` 多了个 s） | 是 **`split`** |
| `ValueError: substring not found` | `index()` 找不到 | 用 **`find()`**（找不到给 `-1`，不报错） |
| `TypeError: sequence item 0: expected str instance, int found` | `join` 的列表里**有非字符串** | 先 `str()` 转换 |
| `TypeError: str.upper() takes no arguments (1 given)` | `upper` 传了参数 | `upper()` **括号里什么都不写** |
| `TypeError: strip arg must be None or str` | `strip` 传了数字 | `strip()` 不写，或写**字符串** |
| `TypeError: find expected at least 1 argument, got 0` | `find()` 括号里空的 | 要告诉它**找什么**：`find("x")` |
| `TypeError: object of type 'int' has no len()` | 对数字用了 `len()` | `len()` 只能量**序列**（字符串、列表） |

## B. 本课语法速查

```python
# 不可变（最重要）
name[0] = "P"          # ❌ TypeError
big = name.upper()     # ✅ 方法返回新串，必须接住

# 找
s.find("o")      s.rfind("o")     # 位置；找不到 → -1
s.index("o")                      # 位置；找不到 → ValueError
s.count("an")                     # 出现几次
s.startswith("2026")   s.endswith(".txt")     # True / False

# 改
s.replace("cat", "dog")           # 全换
s.replace("cat", "dog", 1)        # 只换前 1 个
s.upper()   s.lower()             # 大小写
s.strip()   s.lstrip()   s.rstrip()   # 去首尾空白
s.strip().lower()                 # 链式调用

# 切与拼
s.split()               # 按任意空白切，丢空段   → ['a','b']
s.split(",")            # 按 , 切，保留空段     → ['a','b','','c']
s.split(",", 1)         # 最多切 1 次          → 最多 2 段
",".join(["a","b"])     # 列表 → 字符串        → 'a,b'
"".join(["p","y"])      #                      → 'py'

# 判断（都给 True / False）
s.isdigit()   s.isalpha()   s.isspace()   s.isalnum()

# 格式化
"{} 的分数是 {}".format(name, score)
"{0}{1}{0}".format(a, b)
"{n}".format(n=5)
f"{name}"                         # f-string（同一个东西的简写）

# 字符与编码
ord("a")        # 97      ord("A") # 65      ord("0") # 48
chr(97)         # 'a'     大小写差 32
bin(10)         # '0b1010'  ← 是字符串！带前缀
oct(10)         # '0o12'
hex(255)        # '0xff'

# 字面量
"a\nb"          # 3 个字符（换行占 1 个）
r"a\nb"         # 4 个字符（\ 不再特殊）
"""多行
文本"""
"-" * 30        # 重复 30 次
```

## C. 卡住了怎么办

| 情况 | 做法 |
|---|---|
| 忘了方法名 | 查根目录 `语法速查.md` 第一节 |
| 报错了看不懂 | 先看**最后一行**（错误类型 + 原因），再往上找 `line N` |
| 结果和预期不一样 | **用 `f"[{...}]"` 包起来打印**——空白字符立刻现形 |
| 觉得"应该对"但不对 | **先猜再跑**，别直接改。**改之前先搞清为什么错** |
| 卡超过 30 分钟 | 去看 `第六课-参考答案.md`——但**先写下你卡在哪** |

---

## ✅ 学完请回填

**交作业时，在 `code\` 里随手留一句话**（不用长，一句话就行）：

> **"我卡最久的地方是 ______"**

**从第五课起，这句比代码更值钱**——代码只告诉我"你会不会"，
**这句话告诉我"你卡在哪、为什么卡"**。

**（第五课 d2 那 12 行推理、E2 那句"自检才查出来"，都是这么来的。）**
