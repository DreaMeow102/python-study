# 流水（决策记录）

> **这份文档记录"为什么"**：谁提的、怎么定的、当时说了什么。
>
> | | |
> |---|---|
> | **谁写它** | 教师 AI |
> | **什么时候写** | 每次讨论之后加一条；每课若改了约定或路线，也要加 |
> | **怎么改** | **只追加。** 旧条目不改（错了就在下面加一条更正的） |
> | **谁读它** | **平时不用读。** 只在"这条规则当初为什么这么定"被质疑时来查 |
>
> **规律**：`约定-教师.md` / `约定-学生.md` 写"是什么"，这里写"为什么"。
> **教一节课，永远不需要读这份文档**——如果需要，说明有东西放错位置了。
>
> **编号说明**：早期三次讨论都发生在 2026-09-15，各自的编号从 #1 重新开始，
> 所以**单看数字会有歧义**（有三个 #1）。引用时**必须带讨论批次**，例如「第三次 #22」。
> 从第四次起编号连续（#35~39）。**本次重构之后，新条目改用日期 + 序号。**

---

# 一、本次：文档体系重构（2026-09-21，第四次讨论之后）

> **起因**：第五课批改后发现三笔欠账（`语法速查` 漏了整块循环、已考知识点清单漏了第五课和真题实战一的行、
> 课次表与规则九冲突）。查根因时发现：**不是设计差，是"批改完就以为收尾了"。**
> 于是把 `学习规则.md`（1112 行，一份文件干四件事）拆开重做。

| # | 议题 | 结论 | 谁提的 |
|---|---|---|---|
| **40** | **文档划分原则** | 只按一条线切：**"每课谁写它、多久变一次"**。不按话题切、不按读者切 | 我提议 + 学生拍板 |
| **41** | **拆成 4 份活文档** | `约定`（极少改）· `进度`（每课改）· `语法速查`（每课追加）· `流水`（只追加） | 我 + 学生 |
| **42** | **手册并入约定** | 原《学习规则》§四目录约定 / §七环境 / §八 git → 变成 `约定.md` 的**附录 A/B/C**。理由：两者都是"极少改"，靠话题分边界必然漂移 | 我提议 + 学生同意 |
| **43** | **新增 `模板.md`** | 正文 / 参考答案 / 批改 / 清单 的**格式**写死 | 我提议 + 学生同意 |
| **44** | **新增 `接手.md`** | 给"零上下文"的新教师 AI 的一页入口 | 学生提 |
| **45** | **新增「每课收尾清单」** | 写进 `约定.md` 附 D。**这是补第 37 条那三笔欠账的机制** | 我 + 学生 |
| **46** | **知识点编号（ID）** | 格式 `级别.序号`，和阶梯号对齐。`进度.md` 出 ID，`语法速查` 引用 ID → **两份互为校验** | 我提议 + 学生同意 |
| **47** | **「正文与理由分离」** | `约定` 只留一句理由，长理由/原话进 `流水` | 我提议 + 学生同意 |
| **48** | **规则四·补充：批改归因规则** | 归因"概念没掌握"之前必须先问"当时你怎么想的"；失分点分三类（概念缺口 / 符号看漏 / 交付不细） | **学生触发**（见下面的专题） |
| **49** | **旧文档处理** | 两个旧文件移进 `旧文档\`，**仅历史参考，不要以它为准** | 学生指示 |

## 专题：`bool("0")` 的归因更正（#48 的由来）

> **这是本次重构最重要的一条副产品：教师把一个错误归错了因，学生纠正了它。**

**教师的原始批改**（`真题实战一-批改.md` §三 / §八）：
> "你答 C（`False 和 False`），正确是 A…… **真值判断没内化**。
> 而你自己在第四课 a3 里写过这样一句话…… **你自己的规律能推出正确答案——但你没用上。**"
> → 归因为**提取失败 / 概念问题**。

**学生的更正（原话）**：
> "至于那个 bool 的错误……因为我是**下意识看到的 0 没注意引号**，粗心导致的，
> 我能承认这次错误，**但我认为我的知识总结没啥问题**。"
>
> "**恰恰是因为知识总结没啥问题**，0 本身输出就是 false，但字符串输出是 true——
> **这也是人才会犯的错误，AI 很注意双引号在不在。**"

**为什么学生的解释成立**（教师复核后的证据）：

| 证据 | 说明 |
|---|---|
| 同一张卷子第 7 题 `5 and 0` → **答对** | 说明"`0` 是假"他没问题 |
| 他答的 `False 和 False` | **正好是把他在 a3 写的规律用在 `bool(0)` 上的结果**——答案和他的规则自洽，只是输入看错了 |
| 若是概念没掌握 | 更可能答成 `True 和 False`（把"非空字符串都是真"搞反）——他没搞反 |

**结论**：**教师的归因是错的，已改。** 补救不是"复习真值判断"，是**"读题时把引号念出来"**。

**学生的这句话值得单独存档**：
> **"恰恰因为知识总结没问题，才会犯这个错。"**
>
> ——**规则越准确、越通用，套错输入的时候答案就越一致地错。**
> 而 AI 不会犯这种错（它不会漏看一个引号），**所以这类错误只能靠人自己的检查动作兜住。**

**这是学生第 5 次纠正教师**（前四次：`//` 的定义、步长措辞歧义、出题重复、真题超纲）。

---

# 二、第四次讨论（2026-09-21）· 第六课设计 + 结账

> **起因**：教师问"要不要开始写第六课"，学生回"**你打算怎么做呢？**"——
> 于是先摆方案给学生审，再动手。**这次讨论有个副产品：核文档时查出三笔没结的账。**

| # | 议题 | 结论 | 谁提的 |
|---|---|---|---|
| **35** | **第六课范围** | **全量**：字符串方法（`split`/`join`/`strip`/`replace`/`find`…）**+ 选择题细节**（`format()` 格式说明符 / `ord` `chr` / `bin` `oct` `hex` / 转义 / 原始串 / `*` 重复）。学生的理由：**要高分，选择题细节不能漏** | 我提议 + 学生拍板 |
| **36** | **第六课之后的顺序** | **复习二 → 小项目 → 真题实战二** | 我提议 + 学生拍板 |
| **37** | **复习二时机（规则冲突）** | 以**规则九**为准：**第 6 课后、覆盖第 1~6 课**。**§5.4 课次表原来写「第 7 课后 / 覆盖 1~7」是错的，已改。** | 我查出 + 学生同意补 |
| **38** | **三笔欠账** | **现在就补**（不等第六课）：① `语法速查.md` 补第五课整块循环内容 ② §3.3 补第五课 + 真题实战一的知识点行 ③ 修正 §5.4 与规则九的冲突 | 我查出 + 学生拍板 |
| **39** | **第六课的定位** | 第 4 级。**二级操作题主力（`jieba` 词频 / 文件读写）全部踩在这一课上**——第五课给了循环，第六课给循环真正要加工的材料 | 我 |

## ★ 这次讨论暴露的系统性问题（本次重构的直接原因）

> **记下来，以后每课结束都要自查**：

| 欠的东西 | 谁负责补 | 什么时候补 |
|---|---|---|
| `语法速查.md` 的新课内容 | 教师 | **每课批改后**（承诺 13） |
| 已考知识点清单 | 教师 | **每课批改后**（规则三的执行依据） |
| 阶梯状态 / 覆盖对照表 | 教师 | **每课批改后** |

**第五课这三样全漏了。**
**根因**：把"批改 + 打分"当成了收尾，**漏掉了批改之后的文档同步**。

> **→ 当时的承诺是"从第六课起，文档同步写进收尾清单"。**
> **但那张"收尾清单"当时没有家——它是一句承诺，不是一个东西。**
> **本次重构把它落成了 `约定.md` 附 D。**

---

# 三、第三次讨论（2026-09-15）· 复习课 + 学习模式

| # | 议题 | 结论 | 谁提的 |
|---|---|---|---|
| 1 | 复习课频率 | **每三课一次**（第 3、6、9 课后） | 学生 |
| 2 | 复习课范围 | **累积式**（复习一覆盖 1~3 课，复习二覆盖 1~6 课） | 我 |
| 3 | 复习课形式 | **全新场景，没有一道原题** | 我 |
| 4 | 限时吗 | **不限时**（我原方案限时，**学生否了**） | 学生 |
| 5 | 看资料吗 | **先不看**，卡超 10 分钟可查，**但必须记清单** | 我 + 学生修正 |
| 6 | 复习必交 | **「想不起来」清单 + 三行总结**（后者后改为不做，见 #25） | 我 |
| 7 | 每课收尾 | **写三行总结；卡住时记「卡的过程」**（规则十） | 我 |
| 8 | 加"读代码" | **加**。从第四课起每课至少一题 | 我 |
| 9 | 语法速查 | **建根目录 `语法速查.md`**，只收录已学内容；**学生的易错点单独成节** | **学生提 + 我设计** |
| 10 | 根目录约定 | 从"只有 `学习规则.md`"**改成两个文档** | 学生 |
| 11 | `code\` 分子文件夹 | **不分子文件夹**，接受现状 | 学生 |
| 12 | 第四课时机 | **复习一做完再进** | 学生 |
| **13** | **复习一覆盖是否均衡** | **初版偏第三课（10 题里 7 题）**，学生发现后**补了第一、二课**（甲2/甲3/甲4）→ **13 题** | **学生提问 + 我修正** |
| **14** | **清单由谁写** | **改成教师从学生的代码注释里提取**（学生自己写会忘，且容易"感动自己"） | **学生** |
| **15** | **清单的格式** | **直接写正确知识点，不写"翻第X课第X节"** | **学生** |
| **16** | **三行总结** | **暂缓**，等学生拍板；且学生建议改成"师生一起生成" | **学生** |
| **17** | **第四课顺序调整** | 原计划"循环" → 改成 **"条件判断（if）"**，循环顺延到第五课 | 我（学生提问触发） |
| **18** | **复习一成绩** | **92.5 / 100**，13 题里 12 题达标，**0 处实质概念错误** | 我 |
| **19** | **前三课事后估算** | 第一课 87 / 第二课 88 / 第三课 85（第二课若扣掉我出题重复的责任约 92） | 学生要求 |
| **20** | **路线图改成「能力阶梯」** | **采纳。** 12 级，每级一句"学完你能……"。原知识点清单漏掉 `if`/`for` 就是因为它们**连格子都没有** | **学生**（我提议） |
| **21** | **第四课 = 条件判断** | 确认。`if` 先于循环，因为**循环体里几乎一定要用 `if`** | 我（学生提问触发） |
| **22** | **第 1 段重定义** | 原定义"数字与文本、list/dict/tuple/set、切片、**推导式**"**自相矛盾**（推导式必须先用 `for`）。现改为覆盖阶梯 **1~5 级** | 我 |
| **23** | **补两块原路线没有的** | **字符串方法**（`split`/`join`/`strip`…）和 **第三方库常识**（`jieba`/`PyInstaller`/`pip`）——二级考点，原路线漏了 | 我（对照二级大纲发现） |
| **24** | **考试定位** | Python 只在**二级**考。现在约 **25~35 分**（60 合格）；**走完第 0~12 级 ≈ 二级水平** | 学生提问 |
| **25** | **三行总结** | **不做**（"总结先不做了"）。规则保留，以后想加随时加 | **学生** |
| **26** | **补两块 + 阶梯扩到 14 级** | 新增 **第 4 级 字符串方法**、**第 12 级 第三方库**——对照二级大纲发现原路线整块漏了。**第六课 = 字符串方法** | **学生要求 + 我执行** |
| **27** | **目标定位** | **近期目标 = 过二级**（可测量、有期限、强迫覆盖）。**但二级是地板不是天花板**——不考 NumPy/PyTorch，离 AI 还有一段 | **学生** |
| **28** | **目标提高：不是 60 分** | 学生定：**"我二级也不是只要 60 分，我要能多高多高"**。→ 要求**选择题细节全覆盖、"了解"级内容也不能略、阶梯一格都不能空** | **学生** |
| **29** | **真题实战写成规则** | **规则十一：每 2 课一次真题；第 7 课后做完整限时卷。** 并新增真题记录表 | **学生** |
| **30** | **长期定位** | 学生原话：**"这个以后是我吃饭的东西"** → Python 是**职业工具**，二级只是**中途检查点** | **学生** |
| **31** | **第四课成绩** | **92 / 100**。亮点：`float()` 坑自己解决（第 4 次）、**a3 推翻教师的真值表且更优**、d2 独立推导出第二种闰年实现 | 我 |
| **32** | **规则七第一次执行** | E1 读文档：**Q4 说反了、且没贴文档原文**。暴露"倾向问我而不是读文档"。**结论：规则七有效，但仍需练** | 我 |
| **33** | **规则八第一次执行** | E2 改错题：**完全正确**（找到 `elif` 顺序错 + 补 `else`） | 我 |
| **34** | **必须纠正的自我标签** | 学生第二次说"我算法不行/数学很差"。**已当面纠正**：那是"急着下笔 + 需要多检查"，不是天赋问题 | 我（学生提出） |

---

# 四、第二次讨论（2026-09-15）· 出题策略

> **这一批的核心是被否掉的提案**——记下来，免得以后又绕回去。

| # | 议题 | 结论 | 谁提的 |
|---|---|---|---|
| 1 | 用 C# 类比压缩路线 | **否掉**（"计算机语言在我眼里是不同的东西，我该重新学"） | **学生** |
| 2 | 知识点覆盖 | **全部覆盖，一个不跳** | **学生** |
| 3 | 重复出题 | **每个知识点只考一次**（规则三） | **学生** |
| 4 | 以留痕为准 | 只决定"讲多细"，**不决定考不考**（规则四） | **学生** |
| 5 | 跳题 | **可以跳，但要给理由**（规则五） | 我 |
| 6 | 小项目 | 第 1 段结束后插一个"没有答案"的经历（规则六） | 我 |
| 7 | 读官方文档 | 第四课起，每课一个知识点学生自学（规则七） | 我 |
| 8 | 改错题 | 第四课起，每课一道（规则八） | 我 |

## 专题：C# 类比提案被否的完整经过（#1）

**教师曾提议**：既然学生有 Unity/C# 经验，就用类比压缩路线、跳过"概念相同"的知识点，
甚至先做一张「Python ↔ C# 对照表」代替基础课。

**学生否了，而且理由更硬**：
> "这就像是你会中文和会英文的区别，**计算机语言在我眼里是不同的东西**，我认为我就该重新学。"
>
> "**超纲等于提前弄，提前弄会降低知识度。**"

**结论**：**会 `$"{x:F2}"` 不等于会 f-string。** 知识要在**新语言里重新长一遍**，删掉中间步骤 = 抽掉地基。
**这一条不许再提。**

**C# 经验该怎么用（两个用法，不是捷径）**：
1. **决定"讲多细"**（规则四）：C# 里碰过的讲一遍就够，没接触过的讲细
2. **解释"Python 为什么这么做"**：对比是**理解工具**。比如 `$"{x:F2}"` ↔ `f"{:.2f}"`，
   知道对应关系能帮记住格式符，**但不等于可以少练一遍**

**真正 Python 独有的（必须重点学）**：切片 · 负索引 · list/dict/tuple/set 的字面量语法 ·
推导式 · `*args`/`**kwargs` · 迭代器/生成器 · `with` · `if __name__ == "__main__"` ·
装饰器 · 动态类型的实践含义 · GIL

---

# 五、第一次讨论（2026-09-15）· 基建

| # | 议题 | 结论 |
|---|---|---|
| 1 | 学习平台 | Windows 本机（不上 WSL/VM） |
| 2 | 目录结构 | 每课一个文件夹，根目录只放规则文档 |
| 3 | Git | GitHub 私有仓库 + SSH 密钥 + 代理 |
| 4 | 密码 | **绝不索取、绝不保存**；只用 SSH |

---

# 六、几件值得单独存档的经过

## 6.1 规则三的由来：第二课的病（出题人自己的责任）

**第二课的毛病是机械对应**：讲一个点 → 配一道题 → 下一个点 → 再配一道题。
结果是 20 道互不相干的小题，还互相重复（B2 / B3 / B5）。

**后果链**：重复的题 → 无聊 → 敷衍（硬编码一个答案交差）→ **被误判成"态度问题"**。

**结论（学生的话推出来的）**：
> **"简单不是不能有，主要是重复性。"**
> **"过度的重复有时候会令人疲惫。"**

**所以**：~~宁多勿难~~ → **宁准勿多。每点一题。**
**一道重复题的伤害不是浪费时间——是它会诱发敷衍，把"没练"伪装成"练了"。**
**责任在设计，不在做题的人。**

## 6.2 规则七的判分标准为什么改松（第五课 F1，2.0 → 3.5）

**教师初判**：F1 得 2.0/5。理由：没写代码、Q3 答错、Q5 没答、没贴文档原文。

**学生提出"判分太严格"——教师重读后，学生是对的。**

**学生原话**：
> "**还是保留**（我没说这种题型不行，而是**判分或者判错太过严格**，仅此而已，这种题型保留是没问题的。）"
>
> "这毕竟是**概念理解题**……大部分实际情况，`range` 和 `while` 的使用，很少会直接套在列表里或者直接打印。"

**教师当时把"输出的类似数组的简写"当成了含糊其辞**，其实它答对了：
`print(range(5))` 输出 `range(0, 5)`，**而 `range(0, 5)` 就是 `[0,1,2,3,4]` 的紧凑写法**。
**教师只认字面量，所以判错了。**

**改后的标准**：

| | 旧标准 | **新标准** |
|---|---|---|
| 看什么 | 交付齐不齐 | **理解对不对** |
| 强制写代码 | ✅ | ❌ 不强制 |
| 强制贴文档原文 | ✅ | ❌ 不强制 |
| 没答全 | 不给分 | **按理解程度部分给分** |

> **保留的软提醒（不扣分）**：Q3 那次是**想错了**，说明脑子里的模型有错——**这个靠刷题兜住。**

**并存档**：初判 2.0 的意见保留，**为了留痕**——以后回头看，能看见标准是怎么变的、为什么变。

## 6.3 学生发现出题人错误的四次（这是真正长出来的能力）

| # | 时间 | 学生发现了什么 |
|---|---|---|
| 1 | 第二课 | 教师正文里 `//` 的定义**写错了**（学生提问触发） |
| 2 | 第三课 c3 | 教师正文对**步长**的措辞**有歧义**（"每 2 个取 1 个"） |
| 3 | 第二课之后 | **出题重复**（B2/B3/B5 是同一个知识点的重复） |
| 4 | 真题实战一 | **卷子超纲**——操作题第 2 题用了第五课的 `for`，而卷子声明只覆盖第 1~4 课 |
| **5** | **真题实战一之后** | **批改的归因错了**（`bool("0")` 判成"概念没内化"，实为看漏引号） |

> **第四次的细节**：学生在卷子上直接写——
> `(此题超纲，建议按照0分算，1到4课没有讲循环)`
>
> **他没做那道题，但写下了理由**——这正是规则五（跳题给理由）要求的。
> **而卷子按他的要求原样保留、不改**（他说"超纲就超纲，这确实没法，但卷子设计没啥问题，保留就好"）。
>
> **这已经是第五次了。而这个能力——"验证出题人，而不是相信出题人"——才是真正在长出来的东西。**

## 6.4 规则十二（提交即冻结）的由来

**学生原话**：
> "不用了。作业留痕。我既然说了提交，那就说明我准备以后面对这个刺眼错误的决定，
> 以后我能看见这个错误——**只要我说了提交打分，就说明我这边做好了「冻结」的决定**，
> **你也没见过学生能后面改卷子分数的吧？**"

**教师的判断**：**这个类比是准的。事后改卷子 = 事后改分，是荒唐的。**

**配套调整**：既然提交后不能改，"检查"（`跑一遍`）就必须发生在**提交之前**。
第四课 `b4.py` 扣的 3 分，就是**按提交时的状态**算的——**这才是公平的位置。**

---

# 七、待议（需要双方确认才能变成约定）

| # | 事项 | 现状 |
|---|---|---|
| 1 | **规则三的字面表述**："同一知识点只考一次，**无论隔多久**"——和"遗忘"有冲突。真题实战一里 `/` 和 `//` 混了，正是隔久了模糊 | **未改**。目前的补丁是规则九（复习课）+ 规则十一（真题） |
| 2 | **`/` vs `//` 那次失分的性质** | 教师**没有问过学生**当时是怎么想的，直接在批改里归成"隔久了遗忘"。**按新的批改归因规则，这一条应改记为"疑似符号看漏"，待问** → **已问，2026-09-22 结掉，见 §八**（结论：**确实是隔久遗忘**，不是符号看漏） |
| 3 | **阶梯排序**：第 8 级（明确"二级不考"）排在第 7、9 级（大纲主体）前面 | **未改** |
| 4 | 第四课/第五课批改文件名与命名规范不符（`-批改` vs `-作业批改`） | 已冻结不追改；从第六课起按规范 |
| 5 | **"终点线"（NumPy → 向量化 → PyTorch）只有一行字，没有计划** | 学生目标是"吃饭的东西"，而阶梯走到头只是"会写单文件脚本"。**建议以后补一段** |

---

# 八、`/` vs `//` 的失分性质确认（2026-09-22）

> **这条结掉了 §七 待议 #2。**

**背景**：真题实战一里 `7 // 2` 被答成 `3.5`。旧批改直接归因「**隔久了遗忘**」——**但没问过学生**。
重构时（2026-09-21）按新的规则四·补充，把它改成「**疑似符号看漏 ⚠️ 待问**」。

**2026-09-22 问了。学生原话**：

> 「**我当时确实忘了 `//` 和 `/` 的区别。**」

**结果**：

| 时间 | 谁归的因 | 结论 | 对不对 |
|---|---|---|---|
| 旧批改 | 教师 | 隔久了遗忘 | ✅ 对——**但没问，是碰对的** |
| 重构时（09-21） | 教师 | 疑似符号看漏 | ❌ **猜错了** |
| **2026-09-22** | **问了学生** | **隔久遗忘** | ✅ |

**为什么这条值得存档**：

**同一个错误，两次"没问"的归因，错了一次半**（一次碰对、一次猜错）。

→ **规则四·补充（归因前先问）的价值被这次实测确认了**——尽管它这次的"产出"是**把一个对的结论改错、再改回来**。
**过程比结论重要**：如果不问，这条会一直以「符号看漏」的名义躺着，**而治法会是错的**（去练"念符号"，而不是复习）。

**派生问题**（已记入 `进度.md` §6 待议 #5）：
规则四·补充现在只有三类性质（概念缺口 / 符号看漏 / 交付不细）。
**「隔久遗忘」的治法是"复习 / 刷题"，而「概念缺口」的治法是"补知识"——治法不同 = 不该同一类。**
→ **是否单列第四类「提取失败 / 隔久遗忘」，待定。**

**同时它给 §七 待议 #1（规则三"同一知识点只考一次、无论隔多久"与"遗忘"的冲突）添了第二个实证。**

**附：本次对旧条目的处理**
§七 待议 #2 那一行**原文未改**，只在末尾加了一个指向本节的**状态指针**。
理由：本文档的规矩是"只追加、旧条目不改"，而**加前向指针不擦除任何记录**。
若认为连指针也不该加，下次讨论时定下来。

---

# 九、约定按「约束对象」拆成两份（2026-09-22）

> **学生拍板**。起因是教师提出"规则定得混乱"，学生同意并给了方向。

**学生的判断（原话）**：

> "目前虽然分了各种规则，但是**规则定得比较混乱，比较'没有意义'，而且有时候会混淆**。
> 我认为这些规则可能我们都要重新审视。"
>
> "这个文档是**约束你的，而不是约束我的**……之后可能得新出一个约束我的文档，
> **我会尽量执行，但毕竟我是人，可能没法完全执行**。"
>
> "**人机交互，我认为机和人得分清楚。**"

**教师查出的支撑证据**（约 71 条离散规则陈述，摊在 5 份文档里）：

| 问题 | 证据 |
|---|---|
| **约束对象混在一起** | 约束教师的 ~55 条、约束学生的 ~6 条、双方 ~3 条，全在一份 `约定.md` 里 |
| **同一件事 4 处说** | "不超纲"（规则二 · §三④ · 附E · 接手§4）、"不重复"（规则三 · §三② · 进度§2 · 接手§4）、"归因前先问"（规则四·补充 · 模板§四 · 接手§4 · 模板§六）、"收尾清单"（约定附D · 模板§四 · 接手§7 · 模板§六） |
| **四种东西都叫"规则"** | 原则（§二/附E）· 规则（一~十二）· 流程（附C/附D）· 格式（附A/模板.md） |
| **写了但没核查** | 规则一、二、四、十② 都没有"怎么知道做到了"的动作 |

### 本次决策

| # | 议题 | 结论 | 谁提的 |
|---|---|---|---|
| **50** | **约定按约束对象拆两份** | `约定-教师.md`（约束教师 AI）+ `约定-学生.md`（约束学生） | **教师提议 + 学生拍板** |
| **51** | **拆分程度** | **只做归类，不重新设计**。正文原样搬运，**规则编号保持不变**（一~十二），避免其他文档引用全断 | 教师提议 |
| **52** | **原文件去向** | `git mv` 到 `旧文档\约定.md`（原文一字未改） | **学生指示** |
| **53** | **学生文档的性质** | **它不是评分依据。** 教师**不得**拿它要求、责备或扣分；学生**尽力执行，做不到就写下来，不假装做到** | **学生定** |

### 明确没做的事（留给下一轮）

| 没做 | 为什么 |
|---|---|
| 没有重新设计规则 | 学生："现在不是做决定的时候……到时候我们一个文档一个文档的看" |
| 没有加"怎么核查 / 违反了怎么办"两栏 | 教师提过，**学生未拍板** |
| 没有解决 4 处重复说同一件事 | 同上 |
| 没有把"格式"从规则里彻底分离 | `模板.md` 已在承担，只是没官宣分工 |

### 附：本次对既有内容的处理

1. `进度.md` / `语法速查.md` / `模板.md` / `第六课-正文.md` / `接手.md` 里指向 `约定.md` 的**活引用已全部更新**为 `约定-教师.md`（共 23 处）。
2. **`流水.md` 自身的历史条目一字未改**（#41 / #42 / #45 / #47 等提到 `约定.md` 的地方保留原样——**那是当时的名字，改了就是篡改记录**）。
3. **但本文件头部那句活导航**「`约定.md` 写'是什么'，这里写'为什么'」**已更新**为 `约定-教师.md`——
   理由：它不是历史，是给下一个读者的指路。**若认为连这句也不该改，下次讨论时定下来。**
4. `接手.md` 里那 4 处需要"同时指向两份"的地方（接手顺序、文档地图、禁区 3、开头说明）**已手工改写**，不是机械替换。
