虚拟机的使用（里面是lnuix，终端，新建文件夹test
其中终端里的指令，目前有downloads，cd，rm -rf
创建a.py，并且用vscode打开
for循环写
for i in range（100）；类似的（这部分应该是算法）

vscode，ctrl加飘号开终端


主要课程中注意c的函数命名
其中说明了const、＆在c++中的意义和使用方式

在python中，着重说明了def的意义

python中，加减算法实现，关于缺损值的实现以及c++中实现

用ubuntu建立a.cpp a.py a.out等文件，并熟悉其中的编译和使用


